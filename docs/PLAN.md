# DesignKit 项目计划书

> 更新日期：2026-08-11 ｜ 项目定位：对标美图设计室（designkit.cn）核心能力的 AI 商品图生成平台，
> 面向已知人群的多人系统（管理员建号 **或** 发邀请码让对方自助注册，后者默认关闭），
> 架构上预留对外 SaaS 能力，提供标准 API 供 ERP 等系统对接。
>
> ⚠️ **部署目标已变更（2026-08-11）**：最终形态是**部署到公网服务器**
> （有域名、HTTPS、反向代理、可能开多 worker、面向陌生人开放注册），
> 群晖 NAS 从「最终部署目标」降格为**局域网测试环境**——用来把功能跑顺、
> 把备份练一遍。这一条改变了很多技术取舍的判据：凡是「内网够用」的做法一律不成立，
> 一律按公网标准写（限速要落库以适配多 worker、客户端 IP 要从
> `X-Forwarded-For` 右侧数、前端不许有外部域名依赖、开放注册前必须堵死 SSRF）。
> 群晖那份部署指南保留（[deploy-synology.md](deploy-synology.md)），定位改成测试环境。

---

## 一、项目现状（第一期已完成 ✅）

### 已交付的功能

| 模块 | 内容 | 状态 |
|---|---|---|
| 生成工作台 | 上传商品图（最多4张）→ 选提示词分类或自由模式 → 设尺寸/张数/画质 → 异步生成 → 在线预览下载、回流继续改、本地裁切平台版位（早期的「选模板 + 填变量」流程已在 2.1 改为按分类走，模板只保留给对外 ERP 接口） | ✅ |
| 提示词模板库 | 分类管理、变量系统（文本/下拉）、JSON 批量导入导出、同名覆盖更新、启用停用 | ✅ |
| 生成记录 | 分页画廊、状态筛选、关键词搜索、详情抽屉、重新生成、删除 | ✅ |
| 生图引擎 | OpenAI 兼容接口（官方/国内中转均可，地址+Key+模型可配置）；mock 模拟模式（不花钱跑通全流程） | ✅ |
| 任务系统 | 数据库任务队列 + 线程池 worker，重启不丢任务、失败自动重试、原子领取（多进程安全） | ✅ |
| 对外 API | /api/v1/*：X-API-Key 鉴权、异步任务、三种给图方式（上传/URL/base64）、月额度限制、HMAC 签名回调 | ✅ |
| 账号安全 | 首次登录**服务端强制**改密（不改用不了任何功能）、登录限速防爆破（已改为**落库**的通用限速，多进程/多 worker 安全，429 带 `Retry-After`，阈值由设置项实时控制）、JWT 令牌可撤销、管理员/成员角色 | ✅ |
| 自助注册（邀请码） | 管理员在「邀请码」页发码（12 位随机、可用次数/有效期/张数/备注，只有作废没有删除，能看到每张码被谁用了、什么时候、来自哪个 IP），对方在登录页点「我有邀请码，去注册」自己开户。**默认关闭**，打开前系统强制三条硬前置（关内网访问 + 对外地址可达且公网必须 https + 图片凭证必须开），闸门**双向**、且注册接口每次还会复查一次。三道防刷闸（邀请码本身 / 单 IP 每小时 / 全站每天）。注册链路零外部请求、不发登录令牌、角色写死 member。⚠️ 总开关**目前没有界面控件**，只能用接口打开（README 8.2 给了可复制的命令） | ✅ |
| 前端零外部域名 | 第三方组件与素材（lord-icon 播放器、TagCloud、Lucide、两个 Lottie、平台图标 SVG）全部本地化进仓库，`preconnect` 已删；理由是 CDN 投毒＝以本站身份执行的全站 XSS＝在线用户令牌被读走。附带结果：**完全离线也能用**。出处与许可见 [AUTH_VISUAL_ATTRIBUTIONS.md](AUTH_VISUAL_ATTRIBUTIONS.md) | ✅ |
| 成员账号 | 管理员建号 / 停用 / 重置密码（刻意不提供删除）、「我的账户」页（含「退出其他所有设备」）、退出登录真正清掉取图凭证；**停用一个成员会连带停用他名下所有还启用着的 ERP API Key，启用账号时不会自动恢复**（要到「API 对接」页逐把重开） | ✅ |
| 多用户隔离 | 上传图 / 生成记录 / API Key 全部按人隔离；越权与不存在返回同一个 404；管理员可读不可写（不能替别人花钱） | ✅ |
| 按人计费 | 每个成员一把网关 Key（AES-256-GCM 加密存库，独立 `data/.enc_key`）；`shared` / `per_user` 两种计费方式；找不到 Key 时**绝不回落全局 Key** | ✅ |
| 自动开通生图额度 | 建成员时系统自动到 Sub2API 网关替他建账号、领一把自己的 Key（默认**关闭**，管理员在「系统设置 → 网关自动开通」里填地址/管理员 Key/分组 id 后自行打开）；七状态可重入状态机 + 后台每 60 秒推进 + 只读五项自检（三色灯）+ 全局暂停闸；**开通链路与注册链路彻底解耦**，全线崩溃时表现为「照常登录、生图等管理员手工发 Key」；开通成功后默认**立刻清空**成员在网关的密码；全程不删账号/不删 Key/不碰余额。说明见 [docs/auto-provision.md](auto-provision.md) | ✅ |
| 图片访问鉴权 | `/files` 从裸静态目录改成带鉴权的路由：网页端 httpOnly Cookie、对外 API `?k=&e=&t=` 签名（默认 7 天）；停用 Key / 停用账号 / 退出登录立刻失效 | ✅ |
| 安全加固 | SSRF 防护（可配置内网开关，恒拦云元数据地址）、路径穿越防护、CORS 白名单、密钥打码 | ✅ |
| 部署 | 一键启动脚本（start.sh）、GitHub Actions 自动构建双架构镜像发布到 ghcr.io、compose 一键起、依赖版本锁定 | ✅ |
| 文档 | 用户 README、ERP 对接文档（docs/erp-api.md）、自动开通运维说明（docs/auto-provision.md）、在线调试页（/docs） | ✅ |

### 质量保障记录

- 端到端 + 单元自动化测试全部通过，覆盖：不花钱的生图网关兼容性、跨库兼容、
  出图比例护栏、图片访问鉴权、多用户隔离与按人计费、自动开通状态机
  （可重入 / 降级 / 失败分流 / 「日志里不出现密码和 Key」）、通用限速，
  以及邀请码与自助注册（归一化、并发下的原子扣减、报错不做探测器、
  角色不可从请求体提权）。
  **具体项数以 [tests/README.md](../tests/README.md) 的表格为准**——
  这里不再抄一份数字，抄了必然某天对不上，而「计划书里的数字是旧的」
  这件事只会让人怀疑整份文档
- 两轮多智能体交叉审查（5 视角并行 + 对抗核实），确认的 **48 个问题全部修复**
- 浏览器人工走查全部页面与关键流程

### 技术栈（当前）

- 后端：Python 3.9 + FastAPI + SQLAlchemy 2.0；本机开发用 SQLite（WAL 模式，零安装），
  服务器 / NAS 部署用 PostgreSQL 16，两种库都已跑通，带现成迁移脚本
- 前端：无构建纯 HTML/JS/CSS 单页应用（零 Node 依赖，后端直接托管）
- 部署：本机 `./start.sh`；服务器 / NAS 拉 ghcr.io 预构建镜像（amd64 + arm64），
  下载 docker-compose.yml 与 example.env 两个文件即可 `docker compose up -d`

---

## 二、路线图总览

```
第二期            第三期            第三点五期（当前）   第四期           第五期（可选）
接入真实生图  →  局域网 NAS   →   公网服务器部署   →  ERP 正式对接  →  功能扩展 / SaaS 化
✅ 基本完成       ✅ 已实机部署      ⏳ 进行中           1-2 周(联调)     按需规划
（仅差成本核算）  （现定位为测试环境）（HTTPS/反代/开放注册）
```

---

## 三、第二期：接入真实生图（进行中）

**目标**：从"模拟出图"切换到"真实 AI 出图"，并把提示词库灌进系统。

| # | 任务 | 谁来做 | 状态 |
|---|---|---|---|
| 2.1 | 提示词库接入 | AI 助手 | ✅ 已完成：接入 YouMind 开源库（CC BY 4.0），全量 1.4 万条。**生成方式已改为按分类走**：选分类 → AI 通览该分类标题挑 4 条 → 取全文 → 结合商品改写，不再需要先「采用为模板」。超 700 条的大分类按商品相关度预筛后随机抽样，保证新同步的条目也能被选中 |
| 2.2 | 网关适配 | AI 助手 | ✅ 已完成：默认模型 gpt-image-2、超时 360 秒；发送前自动按所选比例补边（应对网关忽略 size 参数）、透明底合白、HEIC 转码 |
| 2.3 | 填写网关 API | **你** | ✅ 已完成（2026-08-09）：自建网关已接通，真实出图验证通过 |
| 2.4 | 真实出图调优 | **你**试跑 + AI 助手调词 | ✅ 基本达标：吹风机/可乐实测出图满意；已修好错商品、比例失控两类问题；换更多品类可继续微调 |
| 2.5 | 成本核算 | **你**提供计费标准 + AI 助手测算 | ⏳ **仍缺**：需你给出网关计费标准 |
| 2.6 | 出图后处理 | AI 助手 | ✅ 已完成：结果一键回流为下一轮输入、本地裁切导出平台版位（0 次调用）、只补缺失张数（不再整单重跑）|
| 2.7 | 比例放开 | AI 助手 | ✅ 已完成：护栏校验取代白名单，覆盖 1:1/3:4/4:5/2:3/9:16/3:2/4:3/16:9 八个电商版位；画幅措辞按尺寸计算 |
| 2.8 | 透明底出图 | AI 助手 | ✅ 功能已完成（提示词白底措辞改写、输入图透明补边、缩略图防黑块）；⚠️ **当前网关不支持**：实测 gpt-image-2 返回「Transparent background is not supported for this model」，换支持的模型即可用 |

**验收标准**：你的常用商品品类，每个核心模板都能稳定出可用的商品图。

**实测结论（2026-08-09，自建 Sub2API + gpt-image-2）**：
- 画质良好；网关**忽略 size 参数**，按输入图方向吸附比例
- **新放开的比例已实测通过**：竖长的可乐罐 → 3:4 出图 1086×1448（比例 0.750，精确）、
  16:9 出图 1672×941（比例 1.777，精确）。16:9 是最难的一档（竖罐塞进宽画幅），
  说明「按尺寸计算画幅措辞 + 输入图补边」这套控制手段是可靠的
- **该网关不支持 background=transparent**（明确 400 报错，不是静默忽略）
- 网关偶发 502「Upstream request failed」，重试即可——不是配置问题
- 耗时：网关空闲时可快到 20 秒，但实测区间是单张 1~5 分钟（参考图越多越慢），
  所以请求超时默认设成 360 秒
- 「按比例预处理」开关是控制出图比例的唯一手段，**必须保持开启**
- 因为比例由输入图决定，**自定义任意比例可行**（补边到目标比例即可），
  不受 gpt-image 三种官方尺寸限制。✅ 已落地（2.7）：白名单换成
  `backend/app/services/sizing.py` 的护栏校验，现有 8 个电商版位，加新版位只改一处

**风险提示**：
- 中转平台质量参差：接口不稳定/排队慢/悄悄换模型。选平台时优先支持 `gpt-image` 系列
  原生接口、按张计费透明的（当前默认对接自建 Sub2API 网关，模型 gpt-image-2）；系统已做自动重试和参数兼容重试，但平台跑路风险无法技术兜底，先小额充值。
- 生图费用：gpt-image 系列高画质单张成本明显高于低画质，建议日常用 medium、出正式图用 high。

---

## 四、第三期：部署到局域网 NAS（已完成，现定位为**测试环境**）

**目标**：从"你的电脑"搬到局域网内的群晖 NAS，团队成员和 ERP 都能访问。

**现状**：已在群晖 NAS 上实机部署并完成一次版本升级（2026-08-09）。
剩下两件收尾：**备份定时任务**和**验收走查**。

> ⚠️ **定位已变更（2026-08-11）**：群晖 NAS 不再是最终部署形态，而是**局域网测试环境**——
> 在这里把功能跑顺、把备份练熟，正式环境是公网服务器（见下面的第三点五期）。
> [deploy-synology.md](deploy-synology.md) 这份指南继续保留、继续维护，
> 但读的时候要知道：它里面「不要把端口转发到公网」「用 IP 访问就行」这类前提，
> 只在局域网这个语境下成立。
>
> 历史决定（保留备查）：当初选群晖是因为它和生图网关在同一局域网，直连即可，
> 不用把网关暴露到公网。这一点在公网部署之后仍然成立——**要暴露到公网的是
> DesignKit，不是生图网关**；网关那一头继续走内网直连（前提是 DesignKit 本身
> 也能连到那个内网，否则要另外规划）。

| # | 任务 | 说明 |
|---|---|---|
| 3.0 | 确定网关接入方式 | ✅ 已定：方案①部署在同一局域网（群晖 NAS），直连网关。外网访问将来另做内网穿透 |
| 3.1 | 准备机器 | ✅ 用现有群晖 NAS，无需另购服务器（4GB 内存以上舒服） |
| 3.2 | 部署 | ✅ 已完成（实机运行中）：compose 含 PostgreSQL + 健康检查；应用进程非 root 运行（容器先以 root 自动修正数据目录权限再降权，NAS 上不用手工 chown）；镜像预构建，`docker compose up -d` 一条命令；数据迁移脚本已备 |
| 3.3 | 配置调整 | ✅ 已完成。把 example.env 改名为 .env，必改 4 项：POSTGRES_PASSWORD（不填数据库容器起不来）、DESIGNKIT_DATA_LOCATION（图片存哪）、PUID/PGID（数据目录属主）、DESIGNKIT_PUBLIC_BASE_URL（对外访问地址）。启动后再到网页填网关 Key |
| 3.4 | 安全基线 | 云防火墙只放行必要端口；若要公网访问建议套 HTTPS（Caddy/宝塔一键证书）；把「允许内网取图/回调」按需关闭 |
| 3.5 | 数据备份 | ⏳ **方案已写进 README，但 NAS 上的定时任务还没建**——这是当前唯一有「丢数据」风险的缺口。PG 用 pg_dump + 图片用 tar，两者都要（只拷 data/ 会丢库）；`data/.secret_key` 与 `data/.enc_key` 两个隐藏文件必须一起备（后者丢了全体成员的网关 Key 永久解不开） |
| 3.6 | 域名（可选） | 国内服务器绑域名需 ICP 备案（约 1-2 周）；纯内部用 IP 访问可跳过 |

**验收标准**：手机/公司电脑都能打开平台正常生成；重启服务器后数据完好、任务恢复。

---

## 四点五、第三点五期：部署到公网服务器（**当前阶段**）

**目标**：从局域网 NAS 搬到一台公网服务器，有域名、有 HTTPS，
陌生人可以拿邀请码自己注册。

**为什么改**：群晖那套只有内网能访问，同事在外面、ERP 在别的机房都够不着，
也没法把系统给圈子外的人用。改成公网之后，威胁模型整个变了——
以前「能访问到这台机器」本身就是一道门，现在这道门没有了。

| # | 任务 | 状态 |
|---|---|---|
| 3.5.1 | **限速改成落库**（`services/ratelimit.py` + `rate_limit_state` 表），取代原来进程内的字典——公网上要开多 worker，进程内计数等于没限速 | ✅ 已完成 |
| 3.5.2 | **客户端 IP 正确解析**（`deps.client_ip` + `trusted_proxy_hops`）：从 `X-Forwarded-For` **右侧**按信任层数往回数，不取最左段（那段客户端可伪造） | ✅ 已完成 |
| 3.5.3 | **前端零外部域名**：公共 CDN 依赖全部本地化，`preconnect` 删除 | ✅ 已完成 |
| 3.5.4 | **自助注册（邀请码）+ 硬前置闸门**：开放注册前强制关掉内网访问、要求对外地址可达且公网必须 https、要求图片凭证开启；闸门双向 | ✅ 已完成 |
| 3.5.5 | **自助注册总开关的界面控件**（「系统设置 → 自助注册」一节：总开关、单 IP 每小时、全站每日、邀请码两个默认值）。后端设置项与校验都已就绪，**只缺前端这一节**；在此之前只能用接口打开（README 8.2 有可复制的命令） | ⏳ **待做** |
| 3.5.6 | 真正买机器 / 配域名 / 上 HTTPS 反代 / 配 `DESIGNKIT_TRUSTED_PROXY_HOPS` / 收窄 CORS | ⏳ 待做（步骤见 README 第五章「公网服务器上必须做的四件事」） |
| 3.5.7 | 备案（国内服务器绑域名需 ICP 备案，约 1-2 周）；也可先用境外机器或 IP+自签证书过渡 | ⏳ 待做 |

**验收标准**：从公司外网用域名打开是 `https://`；发一张邀请码给一个外部的人，
他能自己注册、登录、并在几分钟内生成第一张图；把 `trusted_proxy_hops` 配错时，
限速的表现能被观察到（说明这一项真的生效了）。

**已知的取舍（写下来免得以后当成 bug）**：

- `allow_internal_targets` 的**默认值故意没改**（仍是 `true`）。改默认会静默打断
  现有的 ERP 内网取图，而要开放注册的人会被闸门强制去关它——这就是闸门存在的意义。
- 群晖那个测试环境**可以继续用 http**：闸门对内网地址（RFC1918 私网 IP、
  无点主机名、`.local` / `.lan` / `.internal` 等）放行 http，只有公网主机才强制 https。
  否则合法的内网用法会被误判成配置错误。

---

## 五、第四期：ERP 正式对接

**目标**：你的 ERP（或其他业务系统）自动调用平台批量生成商品图。

| # | 任务 | 说明 |
|---|---|---|
| 4.1 | 给对接方发资料 | 「API 对接」页创建专用密钥 + 发送 docs/erp-api.md，对方开发按文档接入 |
| 4.2 | 联调支持 | 常见问题：签名校验、回调地址连通性、图片 URL 有效期；平台侧日志可协助排查 |
| 4.3 | 额度与监控 | 给每个对接方设月额度防失控；观察 used_total 用量 |
| 4.4 | 按需增强 | 对接中可能提出的需求：批量提交接口、任务列表查询接口、图片自动回传对方 OSS 等，届时按优先级排 |

**验收标准**：ERP 下单 → 自动生成商品图 → 回调拿图入库，全程无人工干预。

---

## 六、第五期：功能扩展（按业务价值排序，做不做由你定）

对标美图设计室的其余能力，按「电商卖货价值 ÷ 开发成本」排序的建议顺序：

| 优先级 | 功能 | 价值 | 技术路径 | 预估规模 |
|---|---|---|---|---|
| ⭐⭐⭐ | 电商套图（一键多模板批量出图） | 一次出主图+场景图+细节图整套，直接可上架 | 现有引擎上加"模板组"概念，一次提交多任务 | 小 |
| ⭐⭐⭐ | 抠图/换背景/AI 消除 | 高频刚需 | 透明底功能已就绪但**当前模型不支持**（见 2.8），换模型即可用；「涂哪改哪」的蒙版局部重绘待定——需先验证自建网关是否支持 `mask` 参数 | 小-中 |
| ⭐⭐ | 模特穿戴图（服装/饰品上身） | 服装类目核心需求 | 提示词 + 参考图（现架构已支持多图输入），效果依赖模型能力 | 中 |
| ⭐⭐ | A+/详情页长图 | 亚马逊卖家需要 | 多图生成 + 排版拼接（Pillow 拼图） | 中 |
| ✅ 已完成 | 团队多成员账号 | 内部协作 | 已交付：管理员建号/停用/重置密码、我的账户页、按人隔离、每人一把网关 Key（2026-08-10）；建号时自动到网关开号领 Key（2026-08-11，默认关，见 [docs/auto-provision.md](auto-provision.md)） | — |
| ⭐ | 批量任务（Excel 批量提交） | 大卖家效率 | 对外 API 已支持，做个网页批量入口 | 小 |
| 谨慎 | 带货短视频生成 | 效果好但成本高 | 需接视频模型（Seedance/可灵等），另立项评估 | 大 |
| 谨慎 | 海报模板编辑器 | 相当于再造一个稿定设计 | 不建议自研，可评估集成开源编辑器 | 很大 |

---

## 七、远期：对外 SaaS 化（若决定商业化）

架构已预留，届时需要补：

1. **多租户**：
   - 数据隔离 ✅ **已完成**（2026-08-10）：上传图、生成记录、API Key、网关 Key
     全部按人隔离，越权与不存在同样返回 404；`/files` 取图也已按归属鉴权
   - 成员邀请 ✅ **已完成**（2026-08-11）：邀请码 + 自助注册，默认关闭，
     打开前有三条强制前置。管理员建号那条路照旧保留
   - 注册登录（手机号验证码 / 微信 / QQ）⏳ 待做，**本期刻意不做**：
     手机号要备案主体、审核 1-3 天，短信真花钱、被刷就是费用黑洞；
     微信/QQ 要企业资质、周期以周计。所以第一版用邀请码。
     地基已经打好——`auth_identities` 表就是按「一个人可绑多种登录方式」设计的
     （`provider` + `identifier` + `credential`），加一种登录方式是往这张表里
     加一行 `provider`，不用动 `users` 表；`POST /api/web/auth/set_password`
     也仍为「注册用户自行设首次密码」预留着（目前走的是注册时自己设密码这条路）
2. **计费**：充值/套餐、按张计量（quota 字段已就绪）、账单
3. **合规**（国内运营的硬门槛，成本最高的部分）：
   - ICP 备案 + 增值电信业务许可（视模式）
   - **生成式 AI 内容合规**：内容审核（接入审核 API）、生成内容标识、算法备案
   - 用户协议/隐私政策
4. **基础设施**：本地磁盘 → 对象存储(OSS) + CDN、监控告警
   （SQLite → PostgreSQL 已完成：部署已用 PG 16，迁移脚本 `backend/migrate_sqlite_to_pg.py` 已备）
5. **数据库迁移工具**：引入 Alembic 管理表结构变更（多人多环境后必需）

> 建议：内部用出效果、ERP 跑顺之后，先以"给同行代生成"的轻模式验证商业价值，再投入 SaaS 化。

---

## 八、固定注意事项

- **备份**：分两种情况，别搞混——
  - 本机开发（SQLite）：`data/` 目录就是全部数据，**停服后**整目录拷贝
  - 服务器 / NAS（PostgreSQL）：数据库在 docker 卷里，**只拷 `data/` 会丢掉全部模板和生成记录**。
    必须 `pg_dump` 备数据库 + `tar` 备整个 `data` 目录两份，命令见
    README「六、数据在哪里 / 怎么备份」
  - 两种情况都一样：`data/.secret_key`（登录令牌 + webhook 签名）和
    `data/.enc_key`（成员网关 Key 的解密钥匙）这两个隐藏文件必须在备份里，
    `tar` / `cp -r` 整个目录本来就会带上它们，但**要检查一遍**
- **升级依赖**：requirements.txt 已锁版本，升级前先按 `tests/README.md` 跑一遍回归
- **升级 Sub2API 网关之后必须复核自动开通**：这套功能依赖的是网关的实现细节
  （接口路径、409 的语义、Key 能否原样读回、合规版本号），**不是公开契约**，
  网关升一个版本就可能整条链路失效（表现是所有新成员都开不通，老成员照常生图）。
  升级后按 [docs/auto-provision.md](auto-provision.md) 第九节做三件事：
  点一次「测试能不能自动开通」比对两个版本号、跑 `test_provision*.py`、
  建个测试成员真出一张图。若网关某版本给管理端加上了「替用户签发 Key」的接口，
  整条「保管密码 + 代登录」的链路就该废弃（文档里给了两条 grep 命令）
- **自动开通的取值必须在真机上验一遍**：所有「默认值」都是网关源码的默认值，
  不等于那台实例（192.168.31.235）上的现状。上线前请用设置页的自检读一遍真实取值
  （后台模式 / 三个人机验证 / 两步验证 / 合规确认 / 远端账号的并发与分组）
- **密钥**：`.env`、`data/.secret_key`、`data/.enc_key`、API Key 明文只出现一次——
  都不要提交到 GitHub（.gitignore 已配好）。
  `.enc_key` 尤其要单独记一笔：它解的是全体成员的网关 Key，丢了的表现是
  「所有人突然都不能生图」而数据库看起来完好，根因极难查
- **开放自助注册之前，三条硬前置必须先满足**（系统会强制拦，但要知道为什么）：
  关掉「允许对外接口访问内网地址」、「对外访问地址」填成别人真打得开的地址
  （公网必须 https）、「图片链接需要凭证才能打开」保持开启。
  最要紧的是第一条：对外接口的取图/回调地址是**由服务端去访问**的，
  允许内网时它可以指向内网里的任何东西（含生图网关管理端口）。
  今天这条链是断的（对外接口 Key 只有管理员能建），
  但「只有管理员能建 Key」在代码里只是一行判断——`routers/apikeys.py`
  的文件头自己就写着「哪天改成 get_current_user 是一行改动」。
  强制关闭正是因为「断」得太脆弱。完整说明见 README 第八章 8.1
- **套了反向代理就必须配 `DESIGNKIT_TRUSTED_PROXY_HOPS`**。不配的表现是
  「全站所有人共用一个限速桶」，一个人被限速全公司登不上；配大了更糟——
  系统会去信客户端自己伪造的那段地址，登录/注册限速当场作废。宁小勿大
- **前端不许出现外部域名**：不要把本地路径改回 CDN，不要新增 `preconnect`、
  外部字体、统计脚本。升级某个组件＝重新下载覆盖本地文件 + 更新
  `docs/AUTH_VISUAL_ATTRIBUTIONS.md` 里的版本号。自查：
  `grep -rn "https://" frontend/` 的结果里只应剩注释、文档链接和输入框示例
- **提示词是核心资产**：定期用「提示词模板」页的「导出」备份提示词库
