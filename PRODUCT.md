# DesignKit 产品说明

## Users

DesignKit 面向需要持续制作电商商品图的运营、设计协作人员和系统管理员。用户通常在桌面端完成上传商品图、选一个提示词分类（或用自由模式自己描述画面）、调整输出参数、发起生成与下载结果；所有登录用户都能进「灵感库」浏览、搜索内置的开源提示词，并点「用它生成」直接带到生成工作台。管理员额外负责维护模板、生成记录、API Key 和系统设置，以及把灵感库条目「采用」为正式模板、手动同步上游（**模板现在服务于对外 ERP 接口和灵感库采用，网页的生成页已经不再选模板、也没有模板变量**）。界面应让新用户能顺利完成一次生成，也让高频用户在重复操作中保持位置记忆与效率。

**这是一套面向已知人群的多人系统，不是任何人都能注册的公开 SaaS。** 账号有三条来路：① 管理员在「成员账号」页逐个创建（一直可用）；② 管理员在「邀请码」页发一张邀请码、由对方自己注册（**默认关闭**）；③ 对方用手机号 + 短信验证码自助注册（**默认关闭**，且默认还要求同时填一张邀请码）。后两条是并列的，可以只开一条。没有在线支付、没有找回密码邮件、没有微信/QQ 登录。认证身份表按「一个人可绑多种登录方式」设计，目前落地了密码和手机号两种：手机号注册的用户名由系统随机生成（**绝不用手机号或其片段**，那会把号码散播到成员列表、任务记录和日志里），密码是可选的——不设密码就每次收验证码登录。

**短信通道默认是「调试模式」：不连任何外部服务、不发真短信、不花钱**，验证码直接显示在注册页上，用途是在没配阿里云之前先把整条流程走通。真实通道是阿里云短信，需要管理员自己在网页上填 AccessKey、签名和模板 CODE（系统不代填、不写进任何配置文件；Secret 加密落库、界面全星号打码、任何接口都不返回）。这里的取向是**把「花钱」当成一等约束**：每一次「获取验证码」都是一笔真金白银的支出，而这个接口不需要登录、对着整个互联网开着。因此发送前有三层限速（同一手机号的冷却与每日上限、同一 IP 每小时、全站每日总量），**任何一层不过就一条都不发**，且配额在真正调用上游之前就扣掉——调用超时时我们并不知道短信到底发没发出去，「成功才扣」会让攻击者靠制造超时无限花钱。绝不自动重试（短信接口不支持幂等），唯一的例外是签名校验被网关当场拒绝那一种——那能确定一条都没发出去。被刷的最坏后果不是账单而是**签名被判定「疑似恶意」封停**，那时整条通道停摆、重新申请按工作日算，所以限速是这条通道能不能上线的前提，不是调优项。

**调试模式与真实模式的差别必须在界面上一眼可见**，这是硬要求：调试模式下验证码等于白送给页面上任何一个人，手机号验证形同虚设；不标明的话，管理员会一直以为短信发出去了，而用户永远收不到，双方都查不出问题在哪。所以注册页在调试模式下常驻醒目提示，设置页在「注册已开、通道却还是调试模式」时常驻警告并写进日志——但系统**不替管理员关掉**这个组合，因为它分不清对方是在内网试流程还是已经上了公网。

**手机号本身按「不该留就不留」处理。** 限速表是长期留存的，且会为每一个曾经请求过验证码的号码留一行（包括打错的、试探的、从没注册成功的），所以它存的是带密钥哈希而非明文——否则会慢慢攒出一份「所有来过的手机号」名单，而这些人绝大多数系统本就没有理由保存其号码。验证码同样只存带密钥哈希（裸哈希挡不住反查：6 位码只有 100 万种可能），日志里一律 `138****8000`。密钥不在数据库里，只拿到库的人算不出号码。

**这条路上的接口刻意不泄露「某个手机号在不在系统里」**：发码接口对已注册和未注册的号响应逐字一样、也照样真发一条短信（少发这一条能省钱，但省下的钱是拿泄露换的）；登录失败一律回同一句话，不区分「没注册过」「验证码错」「密码错」。唯一的例外是注册时提示「这个号已经注册过，请直接登录」——走到那一步的前提是对方刚提交了一条正确的验证码，也就是说他本来就拿着这个号，对他而言这不是秘密；含混带过反而会让一个忘了自己注册过的人卡在注册页反复重发短信。

**自助注册是一个带硬前置条件的开关，不是一个普通开关。** 这道闸门同时管着上面两条并列的自助注册路（邀请码、手机号）——只要**任意一条**开着，下面三条就必须全部成立；只管住其中一条等于没管，那样管理员可以把邀请码注册关掉、手机号注册打开，再把内网访问改回来，而那正是最危险的组合。手机号那条路还多一条自己的前提：短信通道要么处在不花钱的调试模式，要么阿里云那四项已填齐——否则打开的是一扇「每个人点获取验证码都看到一串英文错误码」的门，而他完全看不出是系统这边没配好。打开它之前，系统强制要求：关掉「允许对外接口访问内网地址」、「对外访问地址」必须是别人真打得开的地址（公网域名还必须 https）、「图片链接需要凭证才能打开」必须开着。三条缺一不可，不满足就存不进去，并给出逐条写明「是哪一条、怎么改」的中文提示；而且是双向的——注册开着的时候也改不回去。这三条里最要紧的是第一条。它防的是这样一条链：对外接口的取图/回调地址由调用方指定、**由服务端去访问**，允许内网时这个地址可以是内网里的任何东西（包括生图网关的管理端口，那上面能建号、改余额、读任意用户的明文 Key）。今天这条链是断的——对外接口 Key 只有管理员能创建，刚注册进来的人拿不到。**强制关闭而不是提醒，正是因为「断」得太脆弱**：「只有管理员能建 Key」在代码里只是一行判断，哪天为了让成员自助创建对接 Key 就接上了，而做那个改动的人不会意识到自己同时打开了内网；把 Key 发给外部对接方、或者被人要走，也是同样的结果。开放注册意味着不再认识系统里的每一个人，此时应当把这道随时可能被绕开的门锁死，而不是依赖别处不出错。系统默认值刻意不动（内网访问默认开着，因为内部 ERP 常在内网），要开注册的人自己去关——闸门存在的意义就在这里。注册链路本身不发任何外部请求、不签发登录令牌、角色写死普通成员，报错也刻意不做成「这串码是不是有效邀请码」的探测器。邀请码只有作废、没有删除：删掉码等于删掉「这张码开过哪些账号」这份唯一的证据。

成员之间数据互不可见——每个人只看得到自己上传的商品图和自己的生成记录，别人的一律显示「不存在」。生图额度按人算：管理员可以为每个成员配置各自的网关 Key，开启后每个人花自己的钱、账单分得清是谁用的；也可以维持默认的「全体共用一把 Key」。管理员为了排障能看到所有人的图，但不能替别人发起生成或补图——那会花那个人的钱。

**每个成员的生图额度是系统在图片网关上为他单独开的账号，用量单独计算。** 这个账号可以由管理员在网关后台手工开好、把 Key 填进来，也可以打开「自动开通」让系统在建号时自己去办（默认关闭）。成员对这件事全程无感：他看不到自己在网关有账号，界面上也不出现网关相关的字眼，只在「我的账户」页看到「可以生图 / 暂时不能生图」，以及（开了「每人一把 Key」时）自己那份额度的余额。**余额是后台定时同步的快照，不是实时值**，页面必须同时显示「这份数据是多久之前的」——把快照当实时值展示，用户会为「我刚花的钱怎么没扣」跑一圈；读不到余额时显示「—」而不是 0，因为「没钱了」和「读不到」在界面上必须长得完全不一样。金额单位固定美元（账在生图网关那边就是按美元记的），系统**不做汇率换算**：编一个汇率进来，页面上的数和账单上的数就成了两个值，对不上时谁也说不清该信哪个。自动开通被明确定位成**省事的优化而不是唯一通路**——它开不通时的表现必须是「所有人照常登录、照常使用，只是生图要等管理员手工发一把 Key」，绝不能变成「新成员进不来」；也因此系统承认会有一小撮成员（例如自己在网关开了两步验证的）永久开不通，那由管理员手工兜底，属于设计的一部分而非缺陷。系统在网关侧只做建号、发 Key、只读冒烟三件事，**不删任何账号或 Key、不调用任何余额变更接口**；也没有「查看成员网关密码」这个功能，任何接口都不返回它。

## Product Purpose

DesignKit 是商品视觉生成与管理工作台：把商品原图、可维护的提示词模板、内置的开源灵感提示词库（约 1.4 万条，后台定期自动同步）和兼容 OpenAI Images API 的生成服务组织成稳定流程，并为 ERP 等第三方系统提供异步任务与回调接口。生成的图片不是公开资源：网页端凭登录状态取图，对外接口凭带签名的链接或 API Key 取图，越权访问一律当作不存在处理。提示词模板与灵感库都只作为风格参考——每次生成前系统会结合用户上传的实际商品与补充要求现场改写提示词，而不是原样套用。生成结果本身也是可继续加工的素材：可一键送回输入位继续微调、可在本地裁切成各平台版位、可只补出缺失的张数。产品成功意味着用户能清楚理解当前输入、下一步动作与生成状态，快速获得可下载的商品视觉结果，同时不因管理功能、异常状态或响应式布局而中断任务。

## Brand Personality

专业、克制、精确。产品应像成熟的视觉生产工具：图像和结果是主角，控件熟悉而安静，信息密度高但不拥挤；AI 能力通过效率、默认值和反馈体现，而不是通过装饰性视觉强调。

## Anti-references

- 不采用通用 AI SaaS 的紫蓝渐变、发光阴影、Emoji 图标、过量胶囊与悬浮卡片。
- 不把生成工作流退化成长表单后台，也不让导航或装饰抢占商品图和结果的视觉权重。
- 不复制美图设计室的 Logo、品牌色组合、会员入口、营销文案、示例商品图或其他品牌资产。
- 不使用错位的基线、随意间距、嵌套卡片、过度圆角和没有业务意义的动效。

## Design Principles

1. **工具优先**：生成页使用稳定的配置面板与结果画布，管理页使用紧凑、可扫描的数据布局。
2. **结果优先**：商品图、生成状态与输出结果获得最大可用空间，说明文字和辅助控制退居次级。
3. **稳定的空间记忆**：导航、配置、主操作与结果在状态切换中不换位，局部更新不造成跳动。
4. **渐进披露**：默认只展示完成当前任务所需的信息，高级参数和危险操作按需展开。
5. **一致即效率**：排版、间距、控件高度、图标和状态反馈跨页面复用同一套组件契约。
6. **零外部域名**：前端不请求任何第三方地址——组件、图标、动画素材全部随仓库分发，按本地路径加载。公共 CDN 被投毒时送来的脚本以本站身份执行，等于全站 XSS、能读走所有在线用户的登录令牌；面向公网开放注册之后这不是可以先放着的风险。附带结果是完全离线（内网、断网、CDN 被墙）也能正常使用，且不向第三方泄露访客 IP。素材出处与许可见 `docs/AUTH_VISUAL_ATTRIBUTIONS.md`。

## Accessibility & Inclusion

目标为 WCAG 2.2 AA（与 docs/UI_REFACTOR_SPEC.md 保持一致）。正文与占位文本需满足对比度要求；所有关键流程支持键盘访问、可见焦点、语义标签和焦点返回；状态变化通过 live region 传达；不只依赖颜色表达状态；触控目标不小于 44×44px；响应式布局不得产生页面级横向滚动；动效必须尊重 `prefers-reduced-motion`。
