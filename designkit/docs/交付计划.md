# 交付计划：从现状到完整交付

> 2026-08-23 制定。依据：全部历史决策（CLAUDE.md 决策 1~41）、三路现状核查
> （代码欠账 / 决策落实 / 运维就绪，逐条读代码验证过，不是凭记忆写的）。
>
> **「交付完成」的定义**：P0+P1 做完 = 内部生产可依赖；P2 做完 = 运营和运维都顺手；
> P3 是「决定给公司外的人用」时才触发的条件项。

---

## P0 生产安心线（钱、数据、安全的硬账）——必须做，约 7~9 天

| # | 事项 | 为什么 | 量 |
|---|---|---|---|
| 1 | **价目表实测 + billing_mode 巡检** | 三档单价至今没实测过（B3：计费档由上游实际返回像素决定，5 个比例各真出 1 张 ≈ $5 才知道落哪档）；B2 点名的巡检断言（gpt-image-2 不许被配成按 token 计价）一直靠人工约定顶着 | 1~2 天 |
| 2 | **备份自动化 + 完整恢复演练** | 现在备份靠手动敲命令；恢复只在临时库演练过提示词部分，图片 tar 恢复没单独验过。做成 NAS cron 每日自动 + 保留策略 + 一次真正的全量恢复演练并记录 | 1 天 |
| 3 | **关掉老 8787**（决策 24） | 它和新系统共用同一个 ChatGPT 账号，Plus 出图额度被双份消耗。⚠ 需你确认关停时机 | 0.5 天 |
| 4 | **Security Scan 清零** | 依赖漏洞扫描红了一周多：后端 govulncheck 有命中、前端 audit 有豁免表外告警。红灯常态化的后果是真出新漏洞没人注意 | 1 天 |
| 5 | **集成测试地基** | designkit 51 个测试全是单元级，9xxx 迁移 SQL 从没在 CI 真数据库跑过（之前 `inconsistent types` 那种错就是这么漏的）；imgsvc 的 selfcheck 也不在 CI。补 testcontainers harness + imgsvc 进 CI | 2~3 天 |
| 6 | **13000 端口收口** | 实测它绑在 NAS 全部网卡上（整个局域网可达），而它的 `/@fs/` 能读项目任意文件。改成默认不起/只绑本机 | 0.5 天 |
| 7 | **监控告警最小闭环** | 现在容器挂了没人知道（只有 restart 自动拉起）。做：NAS cron 巡检（健康端点+磁盘+容器态）→ 通知你（⚠ 渠道需你定：微信/钉钉/邮件）；imgsvc/rembg 补 healthcheck；6 个容器补内存上限 | 1~2 天 |
| 8 | **邮箱白名单 OAuth 绕过收口** | 第三方登录现在全关着所以无实际风险，但差一道保险：加启动断言「第三方登录开启时白名单必须生效」或补上校验调用 | 0.5 天 |

## P1 产品补完（把半成品收口）——约 8~10 天

| # | 事项 | 说明 | 量 |
|---|---|---|---|
| 9 | **对话流式输出** | 现在 AI 回复是攒完整段才显示，长回答要干等几十秒。改成边生成边显示（打字机效果） | 2~3 天 |
| 10 | **推荐结果缓存** | 同一批图+同分类+同特点重复点「重新推荐」，每次都真花 $0.09~$0.34。加缓存后重复点免费 | 1 天 |
| 11 | **keep_transparency 落地** | 对外接口收这个字段但实际忽略（ERP 传 true 拿到的还是白底）。字段已是对外契约，建议实现而不是删：jobs 加列、worker 按批生效 | 1 天 |
| 12 | **放大任务落库** | 现在重启后端会丢正在排队的放大任务（内存队列）。落库后重启接着跑 | 1 天 |
| 13 | **用户记录里对话附图可看** | 现在管理员看别人对话里的图只显示「[附图]」占位。补管理员级图片通道 | 0.5 天 |
| 14 | **「打包下载这一批」** | 批次详情的 TODO：一键 zip 下载整批结果图 | 1 天 |
| 15 | **ERP 回调投递** | 决策 4 的最后一块：callback_url 收下但从不投递。做投递+重试+投递状态列。⚠ 目前没有真实 ERP 对接方——可降级到 P2，需你定 | 2 天 |
| 16 | **仪表盘后门收口** | /dashboard 的快捷按钮直通对运营隐藏的菜单（决策 10 的尾巴）。默认落地页已改成工作台，只剩手输网址可达。藏不藏 ⚠ 需你拍板 | 0.5 天 |
| 17 | preprocess 端点收口 | 前端类型里引用的端点后端一直是 TODO：实现或把引用删干净 | 0.5 天 |

## P2 运营与工程质量——约 5~6 天

| # | 事项 | 量 |
|---|---|---|
| 18 | designkit 前端测试补课：223 个前端测试里 designkit 自己的是 **0 个**，CI 关键集合也不含。补核心场景（张数×单价计算、停止排队两段语义、金额显示） | 2 天 |
| 19 | `dk-nas.sh ci` 一键完整预演（lint + 单测 + 前端三件套——本周 CI 红灯一周的教训） | 0.5 天 |
| 20 | sync 漂移防护：只增不删导致 NAS 掩盖过真问题（tagcloud.d.ts 事件），加漂移检测或白名单 --delete | 0.5 天 |
| 21 | 容器 stdout 日志限额（compose logging 配置，防磁盘被日志吃满） | 0.5 天 |
| 22 | 图片清理开关：expires_at 列早备好了，做管理设置开关+清理任务（硬盘紧了随时能开） | 1 天 |
| 23 | 版本检查彻底关闭（后端仍在轮询上游 releases，决策 36 的尾巴） | 0.5 天 |
| 24 | 域名反代配置入库：www.2bxiaoxin.top → 18080 的配置整段活在群晖 DSM 界面里，仓库零记录——导出成文档章节，机器坏了才能重建 | 0.5 天 |

## P3 对外开放（条件触发：决定给公司外的人用时）

按决策 9/37 现状是「只给自己公司」，这层**先不动**，触发时按序做：

1. 域名正式化 + HTTPS + 备案
2. 人机验证 + 邮箱验证 + 限流收紧 + 邮箱白名单清空（⚠ 白名单留着=外人全注册不了）
3. 管理员密码轮换 + 公网安全复查
4. **分布式锁**（AdvisoryLocker 实现）——多实例/滚动更新的硬前提，否则余额会被结算两遍
5. S3 对象存储落地（现在是刻意的空壳）+ 存储扩展性评估

---

## 需要你拍板的（其余我自己排期推进）

| 问题 | 选项 |
|---|---|
| ① 老 8787 什么时候关 | 现在关 / 你指定时间 |
| ② 告警往哪发 | 微信 / 钉钉 / 邮件（给我对应的机器人地址或邮箱） |
| ③ ERP 回调现在做还是等有对接方 | P1 做 / 降到 P3 |
| ④ 仪表盘后门 | 彻底藏 / 保持现状（只有手输网址可达） |
| ⑤ 节奏 | 按 P0→P1→P2 顺序 / 你指定优先项 |

## 建议节奏

- **第 1 周**：P0 全部（1~8）——做完这周，系统达到「内部生产可依赖」
- **第 2 周**：P1 的 9~14（流式、缓存、几个半成品收口）
- **第 3 周**：P1 剩余 + P2 全部——做完即「完整交付」状态
- P3 挂起，等触发

> 附注两条原则：每一项完成的标准都包含「文档同步 + 设计稿同步（涉及界面时，决策 40）+
> 测试 + NAS 部署验证 + CI 绿」；金额、计费、余额相关的改动一律由主会话亲自写，不分包。
