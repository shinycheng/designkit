# 这个项目的约定

designkit：面向电商运营的 AI 商品图生成平台。
**以开源项目 Sub2API 为底座**（Go + Vue3），在其之上加入生成工作台、
提示词灵感库、按分类检索、出图比例护栏。

使用者是**非技术背景的电商运营人员**，全靠文档操作系统。

---

## 一、代码组织（2026-08-13 重写：这里原来叫「合并纪律」，规则已经变了）

> **⚠ 先读这段，它推翻了本节原来的核心前提。**
>
> monica 2026-08-13 明确：**这是她自己的项目，只是起点是 Sub2API，
> 不是一个要持续跟随上游的 fork。** 以后按她自己的版本节奏走；
> 需要上游某个更新时，她会让我读那次更新的内容、**挑需要的加进来**，
> 而不是整体合并。
>
> **所以：不要再为「以后跟版会不会冲突」绕路。**
> 哪种改法最直接、最好懂，就用哪种，该改上游文件就改。
> 之前为了避开高频变动文件而绕的弯路（比如「宁可改一个从没变过的
> service 也不碰 routes/admin.go」），那套权衡作废了。
>
> 下面保留的两条仍然有意义，但理由变了：
> 1. **新代码放自建目录** —— 不是怕冲突，是为了「哪些是我们写的」一眼可见。
> 2. **记录改过哪些上游文件** —— 等她说「把上游那个新功能加进来」时，
>    这份清单正好指出哪几处会撞上。`designkit/bin/check-upstream-touch.sh`
>    因此从**拦截**改成**清点**：只列清单、不再报错。

**新代码放进自建目录**（为了可读性，不是为了合并）：

| 类型 | 放这里 |
|---|---|
| 后端业务代码 | `backend/internal/designkit/{handler,service,repository}/` |
| 数据表 | `backend/migrations/9xxx_designkit_*.sql` + 手写 repository。**不要建 ent schema**（见下） |
| 前端页面 | `frontend/src/features/designkit/` |
| 界面文案 | `frontend/src/i18n/locales/<语言>/designkit.ts` |
| 脚本、我们的文档 | `designkit/bin/`、`designkit/docs/` |

**⚠ 建目录时避开 `scripts`、`tests`、`vendor` 这三个名字。**
（`vendor` 是 2026-08-23 实测补的：TagCloud 目录被 `vendor/` 规则吞掉，
历史重置时随 `git add -A` 静默丢失，CI 连红一周才发现。）
上游 `.gitignore` 里这两条规则**不带斜杠**，会在任意层级命中同名目录，
把文件悄悄吞掉——不报错、不提示，等 CI 找不到文件时才发现。
`docs/*` 和 `CLAUDE.md` 同理。新建文件后先确认一下：

```bash
git status --porcelain -uall | grep '^??'
```

**新文件出现在这个列表里 = 能提交**（被忽略的文件不会出现）。

> **⚠ 不要用 `git check-ignore -v` 的「有没有输出」来判断**（2026-08-12 实测纠正）。
> 自从 `.gitignore` 末尾加了放行块，`designkit/` 下**每个文件都会打印一行**，
> 而且退出码也是 0：
> ```
> $ git check-ignore -v designkit/imgsvc/app/main.py
> .gitignore:157:!designkit/**   designkit/imgsvc/app/main.py     ← 这是放行，能提交
> $ git check-ignore -v data/designkit.db
> data/.gitignore:7:*            data/designkit.db                ← 这才是真被忽略
> ```
> 真要用 `check-ignore`，得看**输出里的模式开不开头带 `!`**——带 `!` 是放行。
> 直接用上面那条 `git status` 更省事。

**⚠ 我们的表仍然不要建 ent schema。** 这一条跟跟版无关，是它本身就麻烦：
`backend/ent/` 下有 200+ 个生成文件（`client.go`、`mutation.go`、`tx.go`、
`migrate/schema.go` …），新增一个 `designkit_*.go` schema 再跑 `go generate`，
这些文件**全部会被重写**——一次改动几百个文件，review 和排错都没法看。
上游自己新增的表也是这么干的（`usage_billing_dedup` 只有迁移 SQL，没有 ent schema）。
我们的表 = **迁移 SQL + 手写 repository**（只依赖 `*sql.DB`）。

**改上游文件是允许的**（2026-08-13 起）。原来那份「只准碰 5 个文件」的白名单
已作废——它的前提是「要一直跟上游」，而那个前提没了。
现在的做法是：**怎么改最直接就怎么改，但改完要留下记录。**

```bash
bash designkit/bin/check-upstream-touch.sh   # 列出我们改过哪些上游文件（只清点，不拦）
```

**⚠ 有一条技术限制仍然成立**（跟纪律无关，是编译问题）：
不要往 `handler/handler.go` 的 `Handlers` 结构体加字段来接 designkit。
① `designkit/handler` 要用 `h.OpenAIGateway` 就得 import `internal/handler`，
而 `handler.go` 又要 import `designkit/handler` 才能声明字段 → **循环依赖，编译不过**；
② 真正给字段赋值的是 `handler/wire.go` 的 `ProvideHandlers`（wire 生成的），
手加字段也永远是 nil。
后端接入点用 `server/router.go` 的 `registerRoutes` 就好，已经跑通了。

**怎么吸收上游的更新**（2026-08-13 改：不再整体合并）：

**不做定期跟版，也不 `git merge` 上游的 tag。** monica 按自己的节奏走版本。
需要上游某个具体更新时（她会指出来），流程是：

```bash
git fetch upstream --tags
git log --oneline v0.1.175..v0.1.xxx -- <相关路径>   # 先看那次改了什么
git show <commit> -- <文件>                          # 逐个读
```

**读懂之后挑需要的改进来，不要整体合并。** 冲突面用
`designkit/bin/check-upstream-touch.sh` 列出的清单对照——
上面有我们改过的每一个上游文件，正好是要重点看的地方。

`.designkit-baseline` 里的 `v0.1.175` 现在的含义是**「我们从这个版本分出来的」**，
不是「我们跟到这个版本」。除非真的整体升级过，否则不要改它。

**不要打 `v` 开头的 tag。** 上游自带的 `release.yml` 监听 `v*`，
会触发它的 GoReleaser 发布流程（我们没有它要的 DockerHub 密钥，只会失败）。
我们的版本号用 `dk-` 前缀，例如 `dk-1.0`。

---

## 二、已经拍板的事（不要再问，也不要重新论证）

1. 图像处理（HEIC 转码、按比例补边、透明合白、缩略图）**保留 Python 小服务**，
   不用 Go 重写。理由：Pillow 那套踩过坑的行为重写风险高，收益只是少一个容器。
2. 界面**默认中文**；英文每条文案都要有，但措辞不必推敲。
   所有新文案的键名带 `designkit.` 前缀，避免覆盖上游文案。
3. 老 designkit **什么都不迁移**，直接清空——用户账号、生成历史不迁；
   灵感库也不迁，新系统重新从 YouMind 开源库拉一遍（每天更新，重拉更干净）。
   前提已确认：monica 没在老系统里改过提示词、加过模板、标过收藏。
4. **API 优先**（2026-08-12 改，原来写的是「只预留不做」）：
   界面上能点的每一步，都要有一个接口能单独调；**前端自己也走这套接口**，
   不搞「界面走内部捷径、ERP 走另一条」。这一期就把接口做出来。
   配套三件：任务完成**回调通知**、接口文档、**契约测试**（CI 里断言接口
   格式不被改坏，否则以后我们改代码会把 ERP 那边悄悄搞断）。
   ERP 用**单独建的账号**的 API Key——出的图记在它名下、费用单独算，
   跟运营在界面上出的图互不干扰。
   尤其是「查询返回的图片顺序」必须与「按序号取第几张图」严格对应——
   ent 的 edge 默认不保证顺序，查询必须显式写 order by，并有专门测试守住。
5. 许可证 **LGPL-3.0-or-later**，与上游一致。见 `NOTICE.md`。
6. ~~商业授权条款由项目所有者自行处理~~（2026-08-15 决策 37 收窄：仅自己公司用，不存在对外商业授权这回事；代码里同样不做判断、不加限制）。
7. **不做模拟出图（mock）模式。** 见下一节。
8. 老代码保留在分支 `legacy-python`，镜像 `ghcr.io/shinycheng/designkit:v1.0-python`
   是迁移期的退路，不要删。

以下为 2026-08-12 第二轮拍板：

9. ~~先内部用，关闭自助注册~~ **（2026-08-14 改）：开放自助注册，并且以后要放公网。**

   monica 2026-08-14 改口：需要开放注册，而且**以后要放到公网给外人用**。
   配套定的：
   - **邀请码 + 邮箱后缀白名单**两道门槛都开。
     ⚠ 这两个是**「都要满足」**（AND），不是二选一
     （`service/registration_email_policy.go:51` 起）。
     而且**白名单留空 = 放行所有邮箱**，不是「禁止所有」。
     → 现在填公司域名；**真放公网时必须清空**，否则外人一律注册不了。
   - **新用户默认余额保持 0**。人能自己注册进来，但要出图得管理员给额度——
     花钱始终在她手上，机器人刷号也烧不了钱。
   - 「开放钉钉注册」保持关闭：它会**绕过上面两道门槛**。
   - 充值、套餐、支付、推广返佣仍然关着（代码不删）；
     **兑换码菜单现在是必用的**——邀请码在那里生成。

   **放公网前必须补的**（`designkit/docs/配置手册.md` 有专门一节）：
   域名 + HTTPS、备案、人机验证、邮箱验证、限流调紧、改掉管理员密码。
   以及那条硬约束：**只能跑一个后端实例**，想扩容得先补分布式锁。
10. **普通用户只看得到我们的菜单**：生成工作台 / 灵感库 / 我的图片 / 个人资料。
    上游原生给普通用户的十几个菜单（我的密钥、批量生图、用量、兑换码、推广返佣、
    可用渠道、订阅、购买、订单、模型广场）对普通用户隐藏，管理员账号仍看得到全部。
    实现上走「按角色过滤菜单」，不要删上游代码。
11. **第一版一次做全**：工作台 + 灵感库自动同步 + 批量出图 + 对外接口。
12. **批量出图 = 多张商品图 × 多个提示词，任意组合，一次排下去。**
    注意这跟上游自带的「批量生图」（BatchImage，走 Google GCS/Vertex、
    几百上千张、几小时才出）**不是同一个东西**，不要复用它的表和流程。
    因为一次点下去可能是几十张图的钱，提交前必须显示**预估花费并二次确认**，
    且有单次批量条数上限。
13. **部署：先群晖内网跑稳，再上服务器。** 群晖阶段不做域名/备案/HTTPS。
    注意群晖上老 designkit 还在跑，新系统的端口和数据目录必须错开，不许覆盖。

以下为 2026-08-12 摸底之后拍板：

14. **批量提交先冻结余额**，出完按实际张数结算、多退。钱不够直接拒绝提交并提示
    还差多少，**不允许透支**。
    （不这么做就会透支：上游准入检查只看「余额 > 0」，不看够不够这一单。）

    > **⚠ 2026-08-14 更正：原来这里写「复用上游 `usage_billing_repo.go` 的
    > Reserve / Capture / Release 三个 SQL」——那是错的，实际代码没这么做，
    > 也不能这么做。** 四条理由都验实过（`designkit/docs/设计定型.md` 第五节）：
    > ① 上游 `Reserve` 真的从 `balance` 里划钱；
    > ② 上游每张图的准入判据就是 `balance <= 0` → 余额 100、冻结 100 →
    >    **本批次一张也出不来**；
    > ③ `Release` 的防幻影守卫把 hold 键硬编码成 `batch_image_hold:` 前缀 →
    >    我们的键查不到，**钱永久卡在 frozen_balance 且不报错**；
    > ④ `Capture` 在 `actual > hold` 时报错回滚，而计费档向上漂是常态。
    >
    > 实际做法是**方案 D**：完全不碰上游那三个接口，自建 `designkit_holds` 记账，
    > `users.balance` 全程只由上游扣一次。可用额 = balance − SUM(open holds)，
    > 提交时 `SELECT balance FOR UPDATE` 关掉上游那个「50 张并发全放行」的窗口。
15. **取消 = 只是不等了，图照样存进作品库。** 网关是**故意**把上游调用和浏览器连接
    脱钩的（`service/openai_images.go` 的 `detachUpstreamContext`，注释写明有意为之），
    点取消/关网页，上游照样出图、照样扣钱。既然钱花了就把图留下。
16. **运营能看到自己的消费**：工作台角落显示「本月出图 N 张 / 花费 $X / 余额 $Y」
    （金额单位见决策 18）。
    原生的用量/充值/订单菜单仍然隐藏，我们自建一个简化版。
17. **图片永久保留，但过期时间字段现在就建好。** 不自动删；`expires_at` 之类的列
    先加上（现在加成本为零，事后补要写迁移），哪天硬盘紧了开开关就能清。

以下为 2026-08-12 设计评审后拍板：

18. **金额一律显示美元（`$`），不做汇率换算。** 系统底层余额和账单全是美元，
    换算会导致「工作台说还差 12.3 元、个人资料显示 $12.30」这种充错钱的情况。
    文档和界面文案里**不许再出现「元」**。
19. **余额不足要有出路。** 提示写成「余额不足，请联系 XXX」（管理员联系方式，
    部署时填进 `designkit_settings`），旁边一个**「申请额度」按钮**——
    点了在管理员后台生成一条待处理记录。
    理由：充值和兑换码菜单按决策 10 已隐藏，运营看到「余额不足」是死路一条，
    而管理员这边没有任何机制会主动知道有人卡住。
20. **重试：运营自己能点，每张累计最多 3 次**（`max_attempts` 默认 3）。
    点重试必须二次确认并显示单价——**重试一张 = 重新出一张图 = 重新收一次钱**，
    技术上改不了。
21. **「取消」按钮改叫「停止排队」**，弹窗写明：
    「还有 N 张没开始，停止后不再生成也不扣费；正在生成的 M 张会跑完并正常扣费，
    图会保存在作品库。」
    理由：上游出图跟浏览器连接是**故意脱钩**的，已经在跑的停不下来。
    按钮叫「取消」而实际照扣钱，会被投诉「我明明取消了还扣我钱」。

以下为 2026-08-14 拍板：

22. **上游账号是 ChatGPT Plus，走 OAuth 登录，不是 API Key。**
    （之前一直按「中转平台 + API Key」写文档和推理，是错的。）
    影响很实际：
    - **OAuth 账号不读 `credentials.base_url`**（`service/account.go` 的
      `GetOpenAIBaseURL` 只在 `Type == AccountTypeAPIKey` 时读它），
      填了也没用，一律走 OpenAI 官方通道。
    - 出图走的是 `forwardOpenAIImagesOAuth`（`openai_images_responses.go`），
      不是 `/v1/images/edits` 那条；代码里默认模型就是 `gpt-image-2`。
    - ⛔ 账号上的「**仅允许 Codex 官方客户端**」开了会把出图请求直接拒掉
      （我们不是 Codex 客户端），报错只说「访问被拒绝」，极难联想。

23. **没有「每张多少钱」这回事。** Plus 是每月固定 $20 包月，
    OpenAI 不按张收费。所以：
    - 「量出真实单价」这件事**不存在**，之前文档里那一步作废。
    - 系统里的金额是**给运营记的内部账**，不是真花出去的钱。
    - **一张图 = $1**（三档全填 1，Plus 的额度按张算、跟图片大小无关），
      这样余额数字直接就是「还能出几张」。
    - 真正的限制是 **Plus 的出图次数上限**，而这个数**在系统里看不到**——
      它在 OpenAI 那边。撞上限的表现是「之前好好的，突然连续失败」。
    - 给运营的额度按「测出来的月额度 × 80%」。
      ⚠ **不要凭空编一个 Plus 额度数字**——OpenAI 一直在调，编了会害人。

24. **老的 8787 那套要关掉。**（✅ 2026-08-23 已关停：容器 stop + restart=no，数据和镜像保留——决策 8 的退路没动。） 它跟新系统**用同一个 ChatGPT 账号**，
    两套同时开着会共用同一份 Plus 额度、消耗翻倍，而且互相看不见。

以下为 2026-08-14 下午拍板：

25. **第二步「挑提示词」改成 AI 配提示词。** 原来那一版只有一个「自己写一条」的
    手写框，第二步叫「挑提示词」却根本没得挑（monica 原话：「并没有挑」）。
    现在的流程：

    ```
    选一个大致分类（不确定选「全部分类」，AI 自己看图判）
      → 写一句商品特点（**不是提示词**，是关于商品的事实：材质、颜色、想要的感觉）
      → AI 看着商品图，从该分类里挑 5 条参考
      → 合成【一条】最终提示词（可直接编辑）→ 出 【1 张】图
    ```

    她拍板的两条：**5 条合成 1 条、出 1 张**（不是 5 条各出一张）；
    **手写框去掉**，改提示词就在结果框里直接改。

26. **候选怎么挑：两段式，粗筛看全量。**（monica 提的，比我原来的方案好）

    ```
    粗筛  该分类【全部】提示词的标题 → 挑 100 条
    细选  这 100 条的标题 + 简介     → 仔细挑 5 条
    合成  5 条正文全文 + 商品图 + 商品特点 → 一条
    ```

    **为什么必须粗筛看全量**：原来是「按 id 取前 100 条」，
    社交媒体帖子 3978 条里 **3878 条（97%）永远不可能被选中**，运营写什么都没用，
    而且完全静默——界面和日志都看不出来。

    **实测数字**（不要凭空改，改之前先量）：

    | 分类 | 条数 | 粗筛 token | 一次推荐花费 | 耗时 |
    |---|---|---|---|---|
    | 社交媒体帖子 | 3978 | **39,913** | **$0.335** | ~110 秒 |
    | 电商主图 | 428 | 4,391 | $0.093 | ~30 秒 |

    对比出一张图 $1，推荐占 10%~33%。monica 2026-08-14 明确表示可以接受。

    ⚠ **粗筛只发标题，不发简介。** 简介是正文前 200 字符，3978 条乘上去
    ≈ 20 万 token，一次粗筛就要 $1.5，比出一张图还贵。简介留到细选阶段
    （那时只剩 100 条）。

    ⚠ **「简介」不是数据库字段**，是从正文开头提炼的：1707 条正文是 JSON 结构、
    开头带 `"type": "..."`（作者自己写的一句话说明，比标题还准）；
    其余 13338 条是散文，取开头一句。占位符 `{art_style}` 原样保留——
    它本身在告诉模型「这条模板可以换主体」。

    ⚠ **中文关键词搜不到任何东西**：15045 条标题里 **15016 条是纯英文**，
    运营打「汽水」「开衫」「饮料」命中数分别是 0、0、0。
    2026-08-14 早些时候写过一版「用商品特点做关键词预筛」，**从来没生效过**，已删除。

28. **发给模型之前，先把提示词里原有的比例说法清掉，再追加运营选的那个。**

    monica 2026-08-14 发现：AI 合成出来的提示词结尾是「…高分辨率，**方形1:1构图**，
    无文字、无水印」，而她在第三步选的是 **16:9**。系统再追加一句
    「宽高比严格 16:9」——**同一条提示词里两个互相矛盾的比例，谁赢纯粹碰运气**。

    根因：**比例在第三步才选，而提示词在第二步就定了**。灵感库那些电商主图模板
    本身满是 `1:1` / `square`，AI 照抄进来了。

    两处都要做，缺一不可：
    - 合成那一步明确要求模型**不要写比例**（`prompt_suggest.go`）；
    - 发出去之前**再清一道**（`worker/process.go` 的 `stripRatioClauses`）——
      不能只靠上一条：模型不一定听，而且运营也可能从灵感库直接拿一条带比例的词。

    清除判据（满足其一就丢掉那一分句）：含比例记号（`1:1`、`16 : 9`、全角 `3：4`），
    或者**同时**含形状词（正方形/竖版/横版/square…）和谈画面的词（构图/画幅/比例/composition）。

    ⚠ **形状词和画面词必须同时出现才清**。只看形状词会误伤商品描述：
    「正方形包装盒」「方形托盘」说的是**商品长什么样**，清掉就把商品说错了。
    同理「严格还原商品外形与比例」里的「比例」说的是商品比例，不能碰。
    误伤商品描述比比例不对更糟——比例不对是形状不对，商品描述被删是**画出来根本不是这个东西**。

    副作用（好的）：清了之后 `withRatioInstruction` 变成**幂等**的，
    重复施加不会累积。落库仍然必须存原文，这条只是把误用的后果从「越滚越长」降成无害。

27. **推荐用的对话模型是 `gpt-5.6-sol`**，跟出图的 `gpt-image-2` 是同一个
    ChatGPT Plus 账号、同一把内部 Key、同一套计费。
    - 读图（vision）**实测有效**：给一张左蓝右黄的测试图，它正确答出「蓝色、黄色」。
    - 计费编号前缀用 **`dks:`**（出图是 `dki:`），这样「我的消费」能把两者拆开看。
    - ⚠ 编号的 scope 必须**每次推荐都新生成**（ULID），四趟用 0/1/2/3 四个步骤号。
      用商品图 uid 当 scope 踩过：同一张图重复推荐编号完全一样，
      上游幂等表判重复 → **第二次起静默不计费**，功能看着完全正常，只有账目对不上。

29. **「用这张继续生成」**（2026-08-14）：出好的图下面除了「下载」还有一个按钮，
    点了把这张图变成一条新的商品图，接着出下一轮。

    - 服务端直接复制，**图片字节不经过浏览器**（远程使用时省 4MB 往返）；
    - 按 sha256 去重，重复点拿到同一条 asset，磁盘上只有一份；
    - 前端**也要去重**：商品图列表里出现两条一样的 = 张数多算一条 = 多花一份钱。

    ⚠ 前端有个坑踩过：`AssetUploader` 是**单向**组件，它渲染的是自己内部的
    `entries`，`modelValue` 从头到尾没被读过。父组件往 `assets` 数组里塞
    ① 界面上看不见，② 运营下次传图时会被内部列表覆盖、那张图**静默消失**。
    必须调它 expose 出来的 `addExistingAsset`。

30. **登录页的平台标识旋转球**（2026-08-14 补回）：15 个电商平台绕成一个球缓慢自转。
    这是老 designkit 就有的，2026-08-13 移植登录页时漏掉了。

    ⚠ **许可证**：京东 / 拼多多 / Amazon / Walmart 这四家**刻意用文字标不用图形标**。
    Simple Icons（CC0）按商标权利人要求下架了这几条，热链维基百科的图既是外部请求、
    收录许可也不都是自由的（京东那张在英文维基是「合理使用」，本就不该随仓库分发）。
    **不要"顺手补齐"这四个图标。**
    库本身是 `TagCloud`（MIT），vendored 在 `frontend/src/features/designkit/vendor/tagcloud/`。

    ⚠ 三个踩过的坑，都写进了 `vendor/tagcloud/index.ts` 的类型声明里：
    - `useHTML` 必须 true（默认走 innerText，我们传的 HTML 会被**当纯文本显示**）；
    - `useItemInlineStyles` 必须 true（定位是库写成行内样式的，给 false 就平铺不转）；
    - `useContainerInlineStyles` 必须 false（容器要保持 absolute 才能压在下层）。

    ⚠ **`<style scoped>` 标不到 JS 塞进去的元素**。这些标是 innerHTML / TagCloud
    塞的，拿不到 Vue 的作用域属性标记，普通选择器一条都不生效 ——
    现象是「只有文字在转」（SVG 只有 viewBox 没有宽高，CSS 没生效就塌成 0）。
    必须写成 `.dk-auth-logo-cloud :deep(.dk-auth-logo-token)`。

    2026-08-14 monica 决定**删掉左边原来那四张流程卡片**，球独占整个场景区，
    半径相应从 0.31 调到 0.40 倍容器宽度。

31. **兑换码支持批量删除**（2026-08-14）。后端 `POST /admin/redeem-codes/batch-delete`
    和前端 `batchDelete()` **本来就有**（「删除全部未使用」用的就是它），
    缺的只是界面入口，所以没写任何后端代码。

    三条容易漏的：确认弹窗要**写明条数**（勾选是跨页保留的，可能比眼前看到的多）；
    提示语报**后端真实删掉的条数**而不是选中数（后端可能少删）；
    删完**清空选中集合**，失败时**不清空**（让管理员能直接重试）。

以下为 2026-08-15 拍板（设计改版，依据 designkit/docs/ 三份设计手册）：

32. **全站单一深色主题「DesignKit Dark」，含上游后台。** 推翻 2026-08-13 的
    「只留白天模式」（monica：当初那是纯偏好，作废）。浅色彻底移除、不留开关。
    - 令牌名一个不改只换值（《设计手册-v3-定稿.md》3.2 表 = 验收标准）；
      新增 `--dk-image-canvas`（图像衬底中性灰 #17191E，无蓝紫偏色）、
      `--dk-accent-2`（数据点缀青）、`--dk-glow`（氛围光，登录页/官网限定）。
    - **实施走调色板源头而不是选择器覆盖**：上游本有完整深色皮
      （`darkMode:'class'`），改 `tailwind.config.js` 的 dark 色阶→深蓝、
      primary teal→电光紫 #7C6BFF，`style.css` 所有 `@apply primary-*`
      编译期变紫，bridge.css 由 772 行缩到 110 行。
    - `force-dark.ts` 把 `<html>` 钉死在 dark class（17 个上游文件在 JS 里读
      `classList.contains('dark')` 选颜色，CSS 管不到，必须钉 class）。
    - 侧栏主题切换按钮已**删除**（不是隐藏）。
    - ⚠ 别再引用已删的 `force-light.ts` / `light-only.css` / `auth-light.css`；
      driver.js 新手引导压制那段**搬去了 bridge.css**（行为规则，不随主题走）。

33. **品牌与官网**：品牌名就叫 DesignKit（纯英文无中文名）；商业形态是
    **整套系统卖给别的团队自部署**（非自营 SaaS），官网叙事按这个写；
    官网三页（首页/申请试用/帮助中心）**零截图、零真实商标、零真实商品照**，
    视觉全用 1.75px 细线条几何插画。logo 出了 4 个方向待 monica 挑定
    （暂用 A「DK 取景框」占位，mark 在 designkit/design/system/mark-a.svg，
    换这一个文件全站生效）。设计稿在 Claude Design 三个项目 +
    designkit/design/ 目录（同一份，git 里的是权威副本）。

34. **外壳第三次拍板：维持上游同款侧栏。** v3 设计手册里的「顶栏 54 +
    68px 窄图标工具栏」是 8-13 已否过一次的方案，2026-08-15 monica 再次
    选择维持现状、只换深色皮。**别再提第三次。** `--dk-toolrail-w` 令牌
    保留不删（防残留引用算出 0），但不该有新代码用它。

35. **界面文案口径：「产品话，不是助手话」**（monica 2026-08-15：原文案「太 AI 化」，
    已全量重写）。以后写任何新文案都按这个口径：
    - 一条文案默认一句话；确需第二句，第二句只讲怎么办。
    - 删语气词（吧/哦/就行/一下）；不自称「我们」；不写安慰话
      （「不用自己排查」「你不用会写」这类）。
    - 关键警示**内容一个不少、压缩成一句**：钱、停止排队两段语义（决策 21）、
      重试再扣费（决策 20）、超时可能已扣费、AI 推荐等 1~2 分钟且不扣费。
    - 按钮名、菜单名不动（让 AI 推荐 / 停止排队 / 申请额度 / 立即同步…），
      文档和设计稿都引用它们。
    - 完整规则写在 `zh/designkit.ts` 文件头注释里；设计稿里嵌的文案已
      同步替换（v2/v3 设计手册是历史存档，里面的旧文案**不改**）。

36. **GitHub 以独立项目呈现，git 历史已重置**（monica 2026-08-15）。
    仓库门面全部 DesignKit 化：README 是本项目的；上游三份 README、DEV_GUIDE、
    CLA、docs/（除 legal）、assets/、release.yml、cla.yml、GoReleaser 配置已删；
    界面站点名兜底与品牌字样改 DesignKit；main 历史压成单提交重推。
    - **红线（许可证义务，永远不删）**：`LICENSE`、`COPYING`、`NOTICE.md`
      的来源与修改声明、源码里的版权注释。Sub2API 只在这些地方出现。
    - **`docs/legal/` 不能删**：前端 `?raw` import + Dockerfile COPY 硬依赖，
      删了 vite 构建和镜像构建都会挂。
    - **内部标识一律不动**：go.mod 模块路径（1553 个文件 import）、compose
      服务/容器/网络名（NAS 部署链在用）、数据库名（现有库就叫 sub2api）、
      Redis 键前缀、`/v1/sub2api/billing` 路由与 JSON 协议字段、xai OAuth 的
      `referrer=sub2api`、todo-guard 标记。改这些只有破坏没有收益。
    - **刻意没改的品牌字样**：UseKeyModal.vue / BatchImageGuideView.vue /
      dashboard.ts 里生成的第三方客户端配置示例（`SUB2API_API_KEY` 等）——
      是给用户复制的配置文本，改错会让照抄的人配不通，且页面对运营隐藏。
    - 完整历史备份：桌面 `designkit-完整历史备份-2026-08-15.bundle`（73 个引用）
      + 本地分支 `backup/sub2api-full-history`。上游 remote 仍在本地，
      吸收上游更新的流程不变；`check-upstream-touch.sh` 已加 merge-base
      回退（重置后与上游 tag 无共同祖先，直接对 tag 树比）。

37. **只给自己公司用**（monica 2026-08-15，收窄决策 33 的商业形态）。
    不存在「卖给别的团队」这回事了，配套处置：
    - NOTICE 里转录的上游「无商业授权」等四条声明已删（法律上本就不要求转录，
      当初是防商业纠纷的保险，现在没有对外商业行为，保险失去对象）。
      **最低保留清单见第五节，那是底线，跟自用不自用无关**——仓库公开着就算分发。
    - 贡献者 = monica + 一位同事（姓名待她提供，要写进 NOTICE 版权行时再加）。
      上游贡献者不列——历史重置后 GitHub 贡献者列表本来就只有实际提交的人。
    - ⚠ 官网三页（designkit/website/）的「整套系统卖给团队」叙事已不成立，
      首页/申请试用页去留**待 monica 拍板**；帮助中心可转内部手册用。
    - 决策 33 其余内容（品牌名 DesignKit、纯英文、零截图）不受影响。

38. **「AI 对话」页**（monica 2026-08-15 拍板：会话列表保存 / 能发图提问 /
    所有运营可用；页面用 Claude Design 出稿后实施，稿在 product/11）。
    - 走推荐同一条通道（`chat_invoke.go` 进程内重入 ChatCompletions），
      扩了 `History []ChatTurn` 支持多轮；**历史里的图不重发**（每轮重发 =
      同一张图付 N 次 token 钱），带图轮次在历史文本前加「[附图] 」标记。
    - 计费前缀 **`dkc:`**（出图 dki: / 推荐 dks: / 对话 dkc:），scope 照旧
      **每次发送现生成 ULID**（决策 27 的幂等坑）。
    - 上限：单条 4000 字、3 张图、单图 2MB、历史窗口最近 20 条、单趟 60 秒
      （前端 120 秒）。**模型成功之后才落库**——失败的发送不留半截会话。
    - 表：`9002_designkit_chat.sql`（sessions + messages，软删，显式 ORDER BY）。
    - 路由双前缀照旧：浏览器 `/api/v1/designkit/chat/*`；ERP 读挂轻量 Key 鉴权、
      发送挂上游 apiKeyAuth（花钱的要过额度拦截）。

39. **并入三个开源能力 + 试穿实测**（monica 2026-08-16 圈选，依据
    `designkit/docs/调研-电商AI开源项目.md`，各项目许可证/硬件要求均实查过）：
    - **抠图白底**：rembg（MIT）独立容器、CPU 镜像、`rembg s` HTTP 模式，
      默认模型 `isnet-general-use`。⛔ **`bria-rmbg` 模型永久禁用**（权重
      CC BY-NC，内部生产也算商用）；`birefnet` 完整版不给运营（CPU 分钟级）。
    - **高清放大**：Real-ESRGAN 小模型 `realesr-general-x4v3.onnx`（BSD，4.9MB）
      + onnxruntime 并入 imgsvc，512 tile 推理，异步交付（NAS 约 1~1.5 分钟/张）。
      ⚠ CPU 必须 fp32；大模型 x4plus 在 NAS 上 20~30 分钟/张，**不引入**。
    - **违禁词+标题规则**：sensitive-word 词库（Apache-2.0）+ 广告法极限词表，
      Go 侧 Aho-Corasick 自写匹配（不引 Java）；平台标题字数做静态配置表。
    - **虚拟试穿不自部署**（开源权重全是 NC 许可证）：灵感库加「模特上身」
      模板实测 gpt-image-2；不够用再考虑 fal.ai Kolors API（$0.07/张）。
    - 重打光（IC-Light）默认不做，触发条件与接入路径见调研文档。

40. **新界面必须同步 Claude Design 设计稿**（monica 2026-08-16 追问后立规）。
    任何新页面/新按钮上线时，`designkit/design/product/` 里要有对应稿并推送
    Claude Design「DesignKit 产品端」项目，文案与 i18n 逐字一致。
    实现先行时**当天补稿**。后端没有可视化设计这回事——后端设计记录在
    代码注释与 `designkit/docs/`，不适用本条。

41. **管理员可看所有账户的对话与出图记录**（monica 2026-08-23）。
    管理区「用户记录」页：按账户筛选 + 对话/出图两个页签。
    - 接口全挂 `/admin/records/*`，**RequireAdmin 是唯一的门**——service 层
      刻意无归属校验，这组接口绝不能挂到 ERP 组或非管理员路由（有单测钉住）。
    - **软删一致**：运营删掉的，管理员也看不到（不做「翻垃圾桶」）。
    - 对话附图只显示「[附图]」占位（商品图读取是用户态通道，不为看图开管理员口子）；
      **出图结果图走管理员专用 content 通道，能看**。

42. **交付计划启动批次（monica 2026-08-23 五项拍板）**：老 8787 立即关（已执行）；
    告警走「巡检脚本落文件 + 手册说明」的文档化方案（`dk-healthwatch.sh`，
    webhook 空函数留好，将来要推送再接）；ERP 回调**后置**（等真有对接方）；
    仪表盘后门**藏**（非管理员访问 /dashboard 一律重定向工作台，菜单名单同步）；
    节奏按交付计划 P0→P1→P2。P0 同批落地：自动备份脚本、集成测试 harness、
    imgsvc 进 CI、13000 只绑本机（BIND_HOST 拆成 SERVER_BIND/DEV_BIND，
    **热更新开发从此要 SSH 隧道**）、Go 1.26.5→1.26.6（安全补丁）、
    mermaid 原型污染修复、B2 计费巡检（billing_watch.go）、
    OAuth 白名单启动断言（绕过面实为六个提供方，比第七节原记录宽）。

**下面三条是纯技术决定，我定的，记在这里防止以后反复：**

- **接口挂两个前缀，handler 和 service 完全共用**（2026-08-12 核实后修正，
  原来写的「统一 `/api/v1/designkit/*`」行不通）：
  - 浏览器 → `/api/v1/designkit/*`，挂 `jwtAuth`
  - ERP → `/v1/designkit/*`，挂 `apiKeyAuth`（跟 `/v1/images/edits` 同一个命名空间）

  为什么不能合成一条：两个鉴权中间件**都只认 `Bearer`**
  （`jwt_auth.go` 与 `api_key_auth.go` 都是 `EqualFold(parts[0], "Bearer")`），
  靠「JWT 有两个点、API Key 没有」来分流不可靠——Key 前缀是可配置项，
  有人配成带点的就串了。分成两条路径零猜测，还顺带避开 API Key 认证失败的 IP 连坐。
  handler 层只从 `GetAuthSubjectFromContext` 取 UserID，不关心来路。
  **`/v1/designkit/*` 是给 ERP 的外部契约，定了别改。**
- **图片序号从 1 开始**（对接文档面向非技术读者，「第 1 张」就是第一张）。
  **批量展开顺序固定为「商品图外层、提示词内层」**：图1×词1, 图1×词2, 图2×词1…
  运营是按商品思考的。顺序必须显式 `ORDER BY`，并有专门测试守住。
- **灵感库提示词被上游改了措辞或删了，历史记录显示当时的快照**，不跟着变。
  生成时把提示词原文存进任务行。
- **给 ERP 开号：手动开一次，写进运维文档。** 上游后台没有「管理员替用户建 Key」的
  接口（`server/routes/admin.go` 的 api-keys 组只有一条改分组的 PUT）。服务层
  `APIKeyService.Create(ctx, userID, req)` 的 userID 是入参、技术上可以替任意人签发，
  但那是上游刻意没开放的能力，权限和审计要我们自己兜。而 ERP 的号一辈子只开一次，
  不值得为它开这个口子。

---

## 二·五、摸底核实结论（2026-08-12，写代码前必看）

两轮 agent 摸底 + 逐条核实的结果。**下面每条都验过行号**，
带「原以为」的是被推翻的说法，别再按旧理解写。

### A. 接进去的方式

**A1. designkit 自己建数据库连接池。**
`registerRoutes`（`server/router.go:99`）能拿到的只有：`*gin.Engine`、`*handler.Handlers`、
6 个中间件、4 个 service（APIKey/Subscription/Ops/Setting）、`CompositeRouteResolver`、
`*config.Config`、`*redis.Client`——**拿不到 `*sql.DB`，也拿不到 `*ent.Client`**。
想让它传 DB 就得改 `server/http.go` 的 `ProvideRouter` 签名，而那会要求重新生成
`backend/cmd/server/wire_gen.go`（**已入库的上游文件**）。
所以：designkit 从 `cfg.Database` 自己 `sql.Open` 一个池。代价是多一个连接池，
换来零上游改动，且我们的 DB 用法跟上游彻底隔离。

**A2. repository 只依赖 `*sql.DB`，完全不碰 ent。**
照抄 `repository/batch_image_repo.go` 的形态。
（原以为可以用 ent 的 `ExecContext/QueryContext` 透传——那两个方法定义在**未导出的
`config` 结构体**上，靠 `Client`/`Tx` 内嵌才露出来，而且没有 `QueryRowContext`；
`sql/lock` 生成的也不是原生 SQL，是查询构造器上的 `.ForUpdate()`。用不上，别绕。）

**A3. 任务状态流转用悲观锁**（`SELECT ... FOR UPDATE` + 状态迁移白名单函数），
照 `batch_image_repo` 的写法。
（原以为 `batch_image_repo` 的 `version` 字段是乐观锁——**不是**，它只是给外部看的
变更计数器，不参与并发控制。真正的乐观锁范例在
`repository/channel_monitor_v2_repo.go:53` 的 `expectedVersion` 模式，
那个只适合「管理员改配置」这类低频、要给前端返 409 的场景。两种别混用。）

### B. 会咬人的

**B1. 自定义 `request_id` 压到 ≤48 字符**（不是 64）。
`usage_logs.request_id` 是 `VARCHAR(64)`（`001_init.sql:139`），幂等表
`usage_billing_dedup.request_id` 是 `VARCHAR(255)`（`071_*.sql:6`）。
扣费和写账单是**两个独立事务、扣费在前**（`gateway_usage_billing.go:908/929`）。
超长的后果不是截断——是 PostgreSQL 抛 `22001` 让整条 INSERT 失败，
**钱扣了、账单没有**；而 `writeUsageLogBestEffort` 的兜底只是换个 context 再插一次，
同样的原因会再失败一次，然后只打一行日志，**没有告警、没有重试、没有补偿队列**。
上游自己还会在后面加 `client:`/`local:` 前缀，所以要留余量。写单测钉死长度。

**B2. 报价失准的头号风险：渠道定价那行的 `billing_mode`。**
只要某分组对该模型存在一条渠道定价（`ChannelModelPricing`），且它的 `billing_mode`
是 `"token"` **或留空**（空串在 `model_pricing_resolver.go:73` 被默认成 token），
整单就按 input/output token 计价，**按次价完全不参与**，极端情况算成 0 或算成文本价。
（原以为触发因素是「上游返回了 image_output_tokens」——**不是**，跟上游返不返 token
毫无关系，纯粹取决于后台那一栏有没有选对。）
→ 上线前必须有一条巡检断言：`gpt-image-2` 在所有分组下解析出来的 Mode 不为 token。

**B3. 计价档由「上游实际返回的图片像素」决定，不是我们请求里写的 size。**
`ResolveImageBillingSize(inputSize, outputSizes)`（`image_billing_size.go:69`）：
**先看输出**——把所有输出图分档，取其中**最高**的那档（`Source="output"`）；
只有一张输出都分不出档时，才回落到请求的 size（`Source="input"`），
再不行才兜底 2K（`Source="default"`）。
分档规则取最长边：≤1024→1K、≤2048→2K、更大→4K。

**这意味着比例护栏 ≠ 价目表，两者之间隔着一个我们控制不了的变量。**
实测记录过：同一张图请求 1:1，网关返回的是 `1254×1254`——最长边 1254，
落的是 **2K 档不是 1K 档**。网关按自己的规格出图，我们请求什么尺寸它不一定听。
→ **价目表必须靠实测填**：每个比例真出一张图，记下实际像素，才知道落哪档。
这正是「真枪实弹冒烟测试」要干的活（5 个比例 = 5 张图的钱）。
在测出来之前，任何「1:1 最便宜」的说法都是猜的。

（另一条更正：原以为「2K 一律是 1K 的 1.5 倍」——**只在兜底默认价里成立**
（`getDefaultImagePrice` 内部）。我们一旦在分组里填了三档价，**填多少算多少**，
没有隐含加成。报价文案别写死 1.5 倍。）

**B3-1. 出图比例（2026-08-12 monica 拍板；2026-08-14 实现方式被实测推翻）**：
`1:1`（主图）、`3:4`（详情竖版）、`4:3`、`16:9`（banner）、`9:16`。

> **⚠ 「补白边是控制比例的唯一手段」这句话是错的，2026-08-14 实测推翻。**
>
> 实测：输入一张 **554×554 的正方图**、请求 `1:1`，网关返回 **1087×1447（3:4）**。
> 补了白边照样不听。
>
> 三条路的真实情况：
> | 手段 | 有没有用 |
> |---|---|
> | 补白边到目标比例 | 做了，但**单靠它不管用**（就是上面那次） |
> | `size` 参数 | 发的是补边后的真实像素（`554x554`），不是接口认的标准尺寸，**等于没发** |
> | **提示词里写版式** | **有用**。加了之后请求 1:1 返回 **1254×1254**（比例 1.000）✅ |
>
> > **2026-08-23 全量实测**：5 个比例各真出 1 张，输出宽高比全部与请求一致
> （1.000/0.750/1.333/1.777/0.563），**全落 2K 档**。方案彻底验证。

所以现在靠**提示词**控比例：出图前在提示词末尾追加一句
> （`1:1` → 「画面必须是正方形构图，宽高比严格 1:1」），
> 实现在 `worker/process.go` 的 `withRatioInstruction`。
> **只作用于发给模型的那一份，落库的快照仍是运营写的原文**——
> 混同的话历史记录里每条词后面都挂着系统加的话，重试还会被追加第二遍。
>
> 补白边仍然保留（它保证产品不被裁），但它不再是比例的保证。
> `normalize_input_ratio` 这个开关**在新系统里不存在**，那是老 legacy-python 的配置名，
> 别去找。新系统的补边是无条件的，且 Python 侧 fail-closed。

**B4. 上游允许透支，准入检查不做预留。**
扣费第一条 `UPDATE` 带 `balance >= $1` 守卫，命中 0 行时会走**第二条不带守卫的
UPDATE**，余额直接为负（`BalanceOverdrafted = true`）。而且准入检查不预留额度——
50 张并发在第一张扣完之前会**全部放行**看到同一个未扣减的余额。
过度承诺窗口 = 请求在途时长（秒级），跟 Redis 缓存 TTL（5 分钟）是两回事。
→ 这就是决策 14「批次入口必须自己冻结」的原因，**且冻结必须落 DB，不能信 Redis 值。**

**B5. 批量必须自己节流，不能 50 个一起打。**
每用户默认 5 并发，超出排队最多 25 个、等 30 秒，再多直接 429；
每上游账号默认 3 并发（这层是**共享资源**，会连累同账号的其他调用）。
50 张一起冲的实际结果是：5 个在跑 + 25 个排队 + 约 20 个当场 429。
→ designkit 自己排队发送；并考虑给出图账号单独提高 `account.concurrency`。

**B6. 迁移文件一旦跑过就绝对不能再改**——包括改注释、改缩进。
`schema_migrations` 以文件名为主键存 SHA256（`migrations_runner.go:23-29`），
checksum 对不上**整个进程起不来**（只有上游自己历史误改的那 20 来个在豁免白名单里）。
校验和算的是 `TrimSpace` 后的内容，所以只有首尾空白改动是安全的。
改需求一律新加一个编号更大的文件。这条要写进 `designkit/docs/` 的开发说明。
另：9xxx 排在上游 220 之后靠的是**字节序不是数值序**，两年内安全但不能假定永远最后
→ 我们的迁移必须自足（自己 `CREATE TABLE IF NOT EXISTS`，不依赖上游某次迁移的产物）。

**B7. RPM 只能按用户或分组区分，不能按 Key。**
（原以为 API Key 上的 `rate_limit_5h/1d/7d` 是请求数限速——**不是，是按美元的窗口消费
上限**，默认 0=不限。）RPM 三层：`(用户,分组) rpm_override` → `groups.rpm_limit` →
`users.rpm_limit`，默认全 0。
→ 想让 ERP 和运营走不同速率，得把它们放进**不同分组**；
Key 层面能做的是给 ERP Key 设**日消费封顶**防跑量，这反而正合适。

**B8. 面板限流默认不会套到我们头上。**
（原以为 `/api/v1` 组上挂了限流——**没有**，`v1` 组是裸的，各模块在自己的 Register
函数里显式 `.Use(panelRateLimiter.Global())` 才生效。）默认值：普通端点 240 次/分/用户，
`Heavy()` 档 60 次/分，公开端点 300 次/分/IP，超限 429 + `Retry-After`。
→ ERP 每 2 秒轮询 = 30 次/分，挂 `Global()` 也安全；
**但轮询端点绝不能挂 `Heavy()`**（只有 60/分，且跟运营的重查询共用一个桶）。

### C. 图片存哪（**2026-08-14 更正：下面这段原来写反了**）

> **⚠ 原文说「唯一实现是 S3 兼容对象存储，没有本地磁盘选项」「接口层可以复用
> 上游的 `ImageStorage`」——跟实际代码完全相反，照着配 S3 不会有任何效果。**

实际情况：designkit 自建了 `domain.ObjectStore`（Put/Get/Delete/Exists），
**唯一可用实现是本地磁盘** `storage/local.go`（环境变量 `DESIGNKIT_STORAGE_DIR`，
默认 `/app/data/designkit`）。`storage/s3.go` 是**刻意的空壳**——一律返回
`ErrS3NotImplemented`，只保留编译期断言保住签名。装配处（`module.go`）
只调 `NewLocalStoreFromEnv`，**没有任何配置入口能选到 S3**。

上游那个 `service.ImageStorage` 我们**没有用**：它只有一个
`Save(ctx, key, contentType, data) (url, err)`，没有 Get、没有 Delete、
只返回 URL 且 key 不落库，做不了「必须经我方鉴权才能看图」。

→ **`object_key` 由 designkit 自己生成并入库**，格式（以代码为准）：

```
designkit/<年>/<月>/<日>/<job_uid>-<seq>-<attempt>-<image_index>.png
```

日期一律 UTC。`attempt` 和 `image_index` 两个维度**不能省**：
少 attempt → 重试会覆盖旧文件；少 image_index → 一次返两张时第二张写到同一个 key。
**URL 一律运行时生成，绝不入库。**

备份因此是「pg_dump + 单独 tar 那个目录」，见配置手册第 10 步。

### D. 前端

**D1. 菜单过滤在 `AppSidebar.vue` 里按 `isAdmin` 本地过滤，不要走 featureFlags。**
`utils/featureFlags.ts` 文件头写明新增一个 flag 要改 **11 个文件**，其中 10 个是上游文件。
后端 Setting 下发同样退化成这 11 个文件（现成开关里 `backend_mode` 会把运营挡在登录页外，
`simple` 模式是全局部署形态还动计费和存储，都不能用）。

**D2. 默认中文写在 `frontend/src/router/index.ts` 的模块体里**（白名单内），
在 `bootstrap()` 之前同步执行：没有 `localStorage['sub2api_locale']` 就设 `zh`。
放 router guard 也行（零闪），**放我们自己 View 的 `onMounted` 必闪英文**。
`i18n/index.ts` 的 `DEFAULT_LOCALE='en'` 不在白名单，不能改。
另：服务端**不存在**用户语言偏好，别去找，只有浏览器本地那一个。

**D3. 新手引导风险下调。** `useOnboardingTour.ts:541` 明写非管理员直接 return，
**普通运营永远不会自动播放引导**，只有点头像菜单里的重播才跑。
锚点找不到也不崩：轮询 8 秒 → `console.warn` → 气泡照显（无高亮）。
最坏后果是重播的人白等 8 秒只能点关闭。可接受，但最好还是从我们这边关掉。

**D4. 两个组件必须自写，别指望上游。**
`ImageUpload.vue` 是给站点 Logo 用的：base64 dataURL、**默认上限 300KB**、单文件、
无拖拽无多选——商品图上传（多选/几 MB/进度/HEIC）要在
`frontend/src/features/designkit/` 自写。图片灯箱上游**根本没有**（全仓零命中），
基于 `BaseDialog.vue` 自建。
其余可直接复用：`Pagination.vue`、`SearchInput.vue`、`EmptyState.vue`、
Toast（不用引组件，走 `useAppStore`）。

### E. 上游账号怎么填（**2026-08-14 整节作废，见决策 22**）

> **⚠ 这一节原来整段是按「中转平台 + API Key」写的，跟实际用的账号类型不符。**
> monica 用的是 **ChatGPT Plus、OAuth 登录**，不是 API Key。
> 原文里「中转地址这格必须填」「留空会指向 api.openai.com 必然连不通」等等
> **对 OAuth 账号全部不成立**——它本来就该连 `api.openai.com`。

OAuth 账号实际要注意的只有三条：

- **Base URL 那一格不用填**，填了也没用：`service/account.go` 的 `GetOpenAIBaseURL`
  只在 `Type == AccountTypeAPIKey` 时读 `credentials.base_url`。
- ⛔ **账号上的「仅允许 Codex 官方客户端」开了会把出图直接拒掉**（我们不是 Codex
  客户端），报错只说「访问被拒绝」，极难联想。
- `security.url_allowlist.enabled` 保持默认 `false`。
  （原来这里的理由是「开了之后中转平台域名不在白名单里」——那个理由随 OAuth 失效了，
  OAuth 恒连 `api.openai.com`，本来就在默认的十个官方域名里。**结论仍然是保持关闭**，
  只是理由不成立了，别拿旧理由去说服别人。）

上传大小：网关 `gateway.max_body_size` 默认 **256MB**，但整个请求体是一次性读进内存的。
**界面提示写 20MB**，给 Python 预处理服务留余量。

### F. 测试（上游没有现成件，要自建两样）

`testutil/` 只有 gin 上下文和几个业务 fixture，**跟数据库和上游网关都无关**。
假上游是各测试文件里各写各的 `httptest.NewServer`，没有共享封装；
testcontainers 的 harness 是 `package repository` 的 `_test.go` 且函数未导出，
**我们的包 import 不到，只能照抄一份**。
→ ① 抄一份 testcontainers `TestMain` 到 `backend/internal/designkit/repository/`
（带 ApplyMigrations，这样 9xxx 迁移天然被测到）；
② 自建一个 httptest 假网关，返回预置 JSON + 一张真实小 PNG。
构建标签见 `backend/Makefile`：`test-unit` = `-tags=unit`，`test-integration` = `-tags=integration`。

---

## 三、测试

产品里**没有**「假装出图」的模式。但自动化测试也**不许真的调网关烧钱**。
所以分两层：

| 层 | 怎么跑 | 花钱吗 |
|---|---|---|
| 单元 / 集成测试 | 指向本地假 HTTP 上游，返回预置 JSON + 一张真实的小 PNG | 不花 |
| 真枪实弹冒烟测试 | 真调网关出 1 张图，**只在人为显式触发时运行** | 花 |

冒烟测试绝不能挂在「每次提交自动跑」上。

---

## 四、推送前必须同步全部文档

**每次向 GitHub 推送之前，把所有文档对照实际代码核一遍**——不是只改这次
碰到的那一份。核验重点是**照着做能不能成功**：命令、端口、路径、环境变量名、
接口字段名、默认值。

这些一旦和代码对不上，使用者照着敲会失败，而且分辨不出是文档错了还是自己做错了。

> 曾经发生过：README 让用户 SSH 隧道到 5432 端口，但该端口从未对外开放过。

部署文档尤其危险：部署形态是 PostgreSQL + Redis + 应用 +（图像服务）多个容器，
备份恢复那一章**必须实际演练一遍**才算写完。

上游自带的文档已随 GitHub 独立化删除（决策 36），`README.md` 现在是我们的，
要一起核。我们的文档放 `designkit/docs/`（**不能**放 `docs/designkit/`，
上游的 `docs/*` 规则会把它忽略掉；`docs/legal/` 是构建硬依赖，别动）。

---

## 五、其他

- 提交信息**不带 Claude 署名**。
- 文档一律用中文，面向非技术读者：说清楚「做什么」和「为什么」，
  少用术语，命令要能直接复制粘贴。
- 密钥类信息（网关 Key 等）**一律让用户自己在网页界面填**，
  不代填、不写进代码或配置文件。
- 许可证义务的最低保留清单（任何情况下不删）：`LICENSE`、`COPYING`、
  `NOTICE.md` 的来源声明与修改记录、源码里的版权注释。
  上游 README 声明的转录已按 monica 2026-08-15 要求删除（决策 37）——
  那不在法律义务内，别再补回来。

## 六、本机开发

**Mac 上既没有 Docker 也没有 Go**（2026-08-12 实测：Docker Desktop / OrbStack /
Rancher / colima / podman 全都没有，只有 node 和 python3）。
所以本机**不编译、不跑测试**，一律推到群晖上做。

```bash
designkit/bin/dk-nas.sh <子命令>
```

| 子命令 | 干什么 | 什么时候用 |
|---|---|---|
| `sync` | 把工作区同步到群晖 | 一般不用单独跑，下面几个都会先同步 |（⚠ **只增不删**：Mac 上删掉的文件会一直留在 NAS 上，能让 NAS 的检查「假绿」——tagcloud.d.ts 误删后 CI 红了一周而 NAS 全绿就是这么来的。怀疑树不一致时用 `git archive HEAD` 铺一份纯 git 树比对）
| `build` | 在 NAS 上编译后端 | 改完 Go 代码，最快的对错检查 |
| `test` | 跑单元测试 | 改完逻辑 |
| `check` | gofmt + go vet | **推送前必跑**，CI 会卡这两项 |
| `web` | 前端类型检查 + 构建 | 改完 .vue / .ts，CI 也会卡 |
| `up` | 起整套环境 | 第一次，或者 down 之后 |
| **`rebuild`** | **重新构建镜像并替换容器** | **改完代码要在 18080 上看到效果** |
| **`restart`** | **只重启后端** | **改完「商品图设置」里那 9 项** |
| `logs` | 看日志 | 排障 |
| `down` | 停掉 | 收工 |

**⚠ `up` 和 `rebuild` 的区别（很容易搞错，搞错了会以为改动没生效）：**
`up` 只是「把没起来的容器起起来」，compose 配置没变时 Docker 判定「已是最新」，
**不重建**——改了代码它一点反应都没有。
而 18080 上跑的前端是**打包进镜像**的，所以改了 .vue/.ts 必须 `rebuild`；
13000 那个开发服务器挂的是源码、会热更新。**两个端口表现不一致，最容易在这里被骗。**

访问地址：**前端和后端都是 http://192.168.31.235:18080**
（13000 是 vite 开发服务器，只在本地调试用，**绝不能对外**——
它的 `/@fs/` 能读到项目目录下的任意文件）。

**⚠ 构建必须开 BuildKit。** 根目录的 Dockerfile 从第一行起就是 BuildKit 专属语法
（`# syntax=`、`FROM --platform=${BUILDPLATFORM}`、`RUN --mount=type=cache`），
经典构建器一行都跑不了。`dk-nas.sh rebuild` 已经处理了，但前提是**基础镜像已经在
NAS 本地**（这台机器上 BuildKit 拉元数据会走 IPv6 超时，而 `docker pull` 没问题）。
缺了会给出明确提示告诉你 pull 哪几个。

CI（`.github/workflows/backend-ci.yml`）在**每次 push**（任意分支）都会跑
`make test-unit` + `make test-integration` + 前端构建。
⚠ 但**没有一个 dk-nas.sh 子命令能完整预演 CI**：`check` 漏了 golangci-lint，
`test` 只跑 unit，`web` 只跑 build 不跑 eslint 和 vitest。

---

## 七、还没做的事（2026-08-14 盘点）

**这一节是清单，不是计划。** monica 明确说过先不动，但要记下来免得忘。

### 功能缺口

| 缺什么 | 现状 | 后果 |
|---|---|---|
| **ERP 回调通知** | `callback_url` 收下、校验、存库、查询时回显——但**全仓没有任何一处发过请求**。`FinalizeJob` 拿到 `won=true` 只打了一行 Info 日志 | 决策 4 的三件配套只做了一件。防重复发送的地基（`RowsAffected=1` 守卫）已经打好，上面什么都没盖。补它要再写一条 9xxx 迁移（`designkit_jobs` 只有 `callback_url` 一列，没有投递状态/重试次数） |
| ~~ERP 接口文档~~ | **2026-08-16 已完成**：`designkit/docs/ERP接口文档.md`，以代码为准盘点全部端点；设计定型第三节的幻影路径表已加「以新文档为准」提醒 | — |
| ~~管理员看不到额度申请~~ | **2026-08-16 已完成**：管理端「额度申请」页（侧栏带红点），通过=先原子认领再走上游 `AdminService.UpdateUserBalance` 打款（打款失败自动退回待处理）；迁移 9003 加处理备注/金额两列 | — |
| ~~删除素材 / 删除任务~~ | **2026-08-16 已完成**：`DELETE /assets/:uid`、`DELETE /jobs/:uid` 双前缀挂载；批次未结束拒删（防「停止排队」入口消失）；工作台/批次详情有入口 | — |
| ~~运营自建提示词~~ | **2026-08-16 已完成**：「我的提示词」增删改（上限 200 条/人，仅本人可见，youmind 词不可改删）；灵感库入口 + AI 推荐结果「存为我的提示词」 | — |
| **推荐结果缓存** | 没有 | 重复点「重新推荐」每次真花 $0.09~$0.34 |
| **仪表盘是隐藏菜单的后门** | 「渠道状态」已隐藏，但 `/dashboard` 的「快捷操作」四个按钮直通 `/keys`、`/usage`、`/batch-image`、`/redeem`——全是决策 10 要藏的 | 菜单藏了、按钮还在。**monica 还没决定要不要一起藏** |
| **版本更新检查仍指向上游** | 后端 `github_release_service.go` 轮询的是上游 Sub2API 的 releases；脱离上游后「有新版本」提示恒为误导，管理员照着升级会把系统换成上游镜像。前端徽章的仓库常量已改指本仓库（命令变 404，无害化），后端轮询未关 | 管理员界面可能长期显示可升级；关掉轮询是后续活 |
| `keep_transparency` | 是公开接口字段，收下、透传、出图时被忽略（`designkit_jobs` 没这一列，worker 用全局配置且恒为 false） | ERP 传 true 拿到合成白底，接口不报错 |

### 结构缺口

| 问题 | 说明 |
|---|---|
| ~~零集成测试~~（**2026-08-23 已补**：testcontainers harness + 9001~9003 真库迁移 + 首批四组集成测试，imgsvc selfcheck 同批进 CI）原文： | designkit 下 40 个测试文件**全部**是 `//go:build unit`，CI 那句 `make test-integration` 对我们是空跑。9001 迁移 SQL 从没在 CI 的真数据库上跑过。2026-08-14 那个 `pq: inconsistent types deduced for parameter $3` 就是这么漏出去的——**类型推断错误只在 PREPARE 阶段暴露，假 repo 的单测永远碰不到**。要补的是 CLAUDE.md 二·五 F 说的那份 testcontainers harness |
| **只能跑一个后端实例** | `worker.Deps.Locker` 传 nil，`AdvisoryLocker` 没实现。多副本会把同一批次结算两遍、冻结额按张多减一次，**用户余额凭空少一截且不报错**。滚动更新时新旧实例并存那一刻也算多副本。跟「以后放公网」直接冲突 |
| ~~`billing_mode` 巡检~~ | **2026-08-23 已做**：`billing_watch.go` 启动 + 每 6 小时巡检，命中打「配置冲突」日志告警 |
| **没有命令能完整预演 CI** | 见第六节末尾 |
| **开发镜像嵌入陈旧前端 chunk** | 2026-08-15 实测：NAS 开发镜像的二进制里嵌着 255 个 js 资产，其中 112 个是上次构建残留的死文件（index.html 引用图谱只覆盖 143 个）。**服务的内容是对的**，只是镜像变大、排查时会在二进制里 grep 到旧文案造成误判。判定「线上是什么」必须走 index.html 引用图谱，不能 grep 二进制 |
| **Security Scan 长期红灯** | 自 8-16 起两个 job 都挂：前端 `pnpm audit` 有豁免表外的新告警、后端 `govulncheck` 有命中。与功能无关的依赖漏洞类失败，需要专门一轮升级依赖/更新豁免表 | CI 页面常年一个红叉，真出新漏洞时没人会注意 |
| **`imgsvc` 无 CI 覆盖** | 不构建镜像、不跑 `selfcheck.py`。而补边是保证产品不被裁的唯一手段 |
| **邮箱白名单对第三方 OAuth 注册不生效** | 2026-08-23 核实绕过面是**六个提供方**（含 GitHub/Google 自动注册），已加启动断言：白名单+任一 OAuth 同开时日志告警（`oauth_whitelist_warning.go`）。根修（补校验调用）仍待做 |
| **NOTICE.md 与 designkit-guard.yml** | NOTICE.md 声明「除 6 个文件外不修改上游文件」，实际改了 12 个；它还写「CI 会断言未修改」——而 `check-upstream-touch.sh` 退出码恒 0，那个 job 是**永远绿灯的空转**。这是许可证义务，不是可选文档 |
