# 这个项目的约定

## 推送前必须同步全部文档

**每次向 GitHub 推送之前，都要把所有文档对照实际代码核一遍**——不是只改这次
碰到的那一份。范围包括：

| 文档 | 覆盖什么 |
|---|---|
| `README.md` | 本机启动、功能总览、服务器部署、备份与恢复、数据迁移 |
| `PRODUCT.md` | 产品定位与能力边界 |
| `docs/PLAN.md` | 分期计划与完成状态 |
| `docs/erp-api.md` | 对外 ERP 接口契约（**必须与 `backend/app/routers/v1.py` 逐字段一致**） |
| `docs/deploy-synology.md` | 群晖 NAS 部署步骤 |
| `docs/UI_REFACTOR_SPEC.md` | 前端界面规范 |
| `docs/AUTH_VISUAL_ATTRIBUTIONS.md` | 登录页视觉素材出处（版权合规） |
| `tests/README.md` | 测试怎么跑（新增测试文件要在这里登记，含项数） |
| `example.env` / `.env.example` | 部署配置模板 / 本机开发配置模板 |
| `docker-compose.yml` 的注释 | 编排里每项设置的理由 |

核验重点是**照着做能不能成功**：命令、端口、路径、环境变量名、接口字段名、
默认值。这些一旦和代码对不上，使用者照着敲会失败，而且分辨不出是文档错了
还是自己做错了。

> 本项目的使用者是非技术背景的电商运营人员，全靠文档操作系统。
> 文档写得啰嗦但准确没关系；写得漂亮但对不上代码就是事故。
> 曾经发生过：README 让用户 SSH 隧道到 5432 端口，但该端口从未对外开放过。

## 其他约定

- 提交信息不带 Claude 署名。
- 文档一律用中文，面向非技术读者：说清楚「做什么」和「为什么」，
  少用术语，命令要能直接复制粘贴。
- 密钥类信息（网关 Key 等）一律让用户自己在网页界面填，
  不代填、不写进代码或配置文件。
